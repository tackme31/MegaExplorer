# 完了ログ（`/evolve` ループ）

`docs/ROADMAP.md` から消えた項目の行き先。**1 項目 = 1 行**、それ以上は書かない。

- ループはここに 1 行足すだけで、`docs/PROGRESS.md` には書かない（役割分担は `CLAUDE.md` の
  「ループエンジニアリング」節）。
- **メモ欄には「実機で確認が残っている観点」を必ず書く。** ループはビルド・テスト・レビューまでしか
  できないので、人間が後で見るときの入口はこの列になる。確認が済んだら `済` と追記してよい。
- 設計判断を含むサイクルだったときも、ここには 1 行しか書かない。「後で `PROGRESS.md` の
  フェーズにまとめるか `docs/investigations/` の STUDY にするか、人間が決める」という印だけ
  メモ欄に残す。

| 完了日 | 項目 | サイズ | コミット | メモ |
|---|---|---|---|---|
| 2026-08-20 | ローカルファイルを開く | S | `evolve/052` | **実機確認が残っている**（`drive` は見送り——事前判定が `ask`＝無人で、確かめたいのが「外部アプリが開くこと」というウィンドウの外の出来事でスクショに写らないため。GUI 確認は起動＋スクショ `.screenshots/565-evolve-052.png` のみで、クラッシュ・白画面が無いところまで確認）: ① ローカルフォルダを紐付けたうえでファイルを右クリックし、"Open local file" が "Download" の下・"Open local location" の上に出るか ② 実在するローカルの対応物が**関連付けプログラムで開く**か（.txt/.jpg/.pdf など、スペースや日本語・中国語を含むパスでも）③ ローカルに対応物が無いファイルでトースト「Couldn't find this item locally — check the linked folder in Settings」が出るか ④ 拡張子に関連付けが無いファイルでトースト「Couldn't open this file」が出るか（Windows の「プログラムから開く」ダイアログが出て `openUrl` が成功扱いになる可能性があり、その場合はトーストが出ないのが正しい挙動）⑤ **フォルダには出ない**か、複数選択では出ないか、ゴミ箱では出ないか ⑥ お気に入り／「最近」画面の行でも効くか ⑦ 紐付けを Clear すると 2 項目とも消えるか ⑧ アイコン（`` OpenFile）が□にならず 16px で見られるか（未検証、`/ui-style` 側）。**フォルダを対象から外す案を採用**（`ActionTarget::FilesOnly`。理由: フォルダを関連付けで開くとエクスプローラーが開くだけで、`evolve/050` の "Open local location" と実質同じものが 2 行並ぶ）。**`ActionArity::SingleOnly`**（理由: ハンドルごとに非同期の `getPath` が走るので、対応物の無い複数選択が件数ぶんのトーストを出す。`Download` の `Any` とはここが違う）。**解決失敗のトーストは `openLocalLocation` の文面を共有し、シェルが開けなかったときだけ既存の `openFile`（"Couldn't open this file"）に振り分けた**（理由: 前者は原因も答えも"Open local location" と同一、後者はローカルに物がある時点で別の失敗）。**`openLocation` と共通の private ヘルパ `withLocalPath` に寄せた**（理由: 解決→GUI スレッド復帰→失敗トースト、が完全に同じ。新しい抽象ではなく 1 クラス内の関数）。レビューはリンタ（`.cpp`/`.h` 4 ファイル、**指摘 0 件**）＋スレッド安全性・所有権のエージェント 1 体（確信度 80 以上の指摘 **0 件**。内側ラムダが `QString`/`std::function` をムーブでなくコピーしているという閾値未満の指摘のみで、`std::function` に入る側は非 mutable ＝キャプチャが const なのでムーブにするには `mutable` が要り、割に合わないと判断して不介入）。テスト 3 件（C++）+ 2 ケース（QML）追加、ctest 733 件すべて通過・警告 0 |
| 2026-08-19 | セッション復元のログインが QML の構築を待たない | S | `evolve/051` | **実機確認が残っている**（`drive` は使っていない——確かめたいのが起動の速さで、マウス操作では測れないため。GUI 確認は起動＋スクショ `.screenshots/564-evolve-051.png` のみで、テストアカウントのツリーが出るところまでは確認済み）: ① セッション復元での起動が体感で速くなったか（**ウィンドウが出てから一覧が出るまでの間**が縮んでいるはず。MEGA 分の約 1.5 秒が QML 構築の約 3.75 秒の裏に隠れる想定）② ウィンドウが出た直後に一覧が出るか、ログイン画面が一瞬見えてから切り替わるのが残っているか ③ 保存セッションが無い状態（ログアウト後）で起動すると今までどおりログイン画面が出るか ④ セッションが失効／破損しているときにログイン画面へ落ちるか ⑤ ログアウト→再ログインでツリー・クイックアクセス・タブが今までどおり読み直されるか（`signedInContentLoaded` の戻しが効いているか）⑥ ツリーパネル幅など `Settings` の復元値が今までどおり効いているか。**`restoreSession()` を `loadFromModule()` の前へ動かすだけの案を採用**（理由: 行が指した直列の解消そのもので、SDK 側は既に非同期なので新しい機構を足さずに済む。詳細はコミット本文）。**`Main.qml` に初期状態からログイン済みを拾う経路を足した**（`loadSignedInContent()` ＋ `signedInContentLoaded` フラグ。理由: 行が指定した確認事項。ただし**レビュー 2 体とも「`invokeOnGuiThread` が常に `Qt::QueuedConnection` なので 状態変化は必ず `app.exec()` 後＝QML 構築後に届き、この経路は現状デッドコード」と指摘**したため、コメントを「ガードであって生きた経路ではない」と書き直したうえで残した——順序を保証するものはどこにも無く、フラグが二重ロードを防ぐので害が無いという判断）。**テストは追加していない**（`main.cpp` は合成ルート、`Main.qml` は QML テストランナーにコンテキストプロパティが無く実体化できないため。`tst_MainDialogs.qml` 冒頭が同じ理由を書いている）。レビューはリンタ（`main.cpp` 1 件 = `INC-2` の include 順、既存行なので不介入）＋エージェント 2 体（スレッド安全性・所有権／QML 状態、確信度 80 以上の指摘 **0 件**）。**QML レビューが「`Settings` ブロックが content `Loader` より上にあることが今回から load-bearing になった」と指摘**（下へ動かすとツリー幅が既定の 240px に戻る）——現状は到達不能な経路なのでコメントは足していない。ctest 730 件すべて通過・警告 0 |
| 2026-08-19 | ローカルの場所を開く | M | `evolve/050` | **実機確認 一部済**（`drive` は事前判定が最初 `skip`＝無人だったので見送ったが、途中で人間が着席して `attended` になったため実行。設定ダイアログに Local folder 行（読み取り専用のパス欄＋Choose…＋Clear、未設定時は "Not linked" で Clear が灰色）が出ること`.screenshots/547`、Choose… でネイティブのフォルダ選択が正しい表題で開くこと `548`、**未リンク時にファイルの右クリックメニューへ "Open local location" が出ない**こと `551` まで確認。起動時は `544`）: ① フォルダを紐付けたあと右クリックに "Open local location" が**現れる**か ② 実在するアイテムでエクスプローラーが開き、**その項目が選択された状態**になるか（ファイル・フォルダ両方、スペースや日本語・中国語を含むパスでも）③ ローカルに対応物が無いアイテムでトースト「Couldn't find this item locally — check the linked folder in Settings」が出るか ④ お気に入り／「最近」画面の行でも同じく効くか、ゴミ箱では**出ない**か ⑤ 複数選択では出ないか ⑥ Clear で紐付けを外すとメニュー項目が消えるか ⑦ アプリを再起動しても紐付けが残るか ⑧ アイコン（`` FolderOpen）が□にならず 16px で見られるか（未検証、`/ui-style` 側）⑨ ライトテーマでの Local folder 行の見た目。**パスの持ち主は Main.qml の `Settings`、C++ 側は写しを持たない案を採用**（理由: テーマ設定と同じ分担で、新しい永続化機構を足さずに済む。詳細はコミット本文）。**メニュー項目の出し分けは QML の `ActionCatalog.isAvailable` を新設して行った**（理由: 「紐付いているか」はアプリ全体の設定で、`MenuContext` にその軸が無く、足すと全タブへのファンアウトが要る。`enabled` と対になる形にした）。**灰色ではなく非表示**（行の指示どおり）。**エクスプローラー起動は `QProcess::setNativeArguments` で `/select,"<path>"` を渡す形**（理由: `setArguments` だと 1 トークンなら `/select` の手前から引用符が付き、2 トークンならスイッチとパスの間に空白が入る。どちらも explorer.exe が解釈しない）。**リンク先が MEGA と一致するかは設定時も実行時も検証しない**（行の指示どおり。存在チェックのみ）。レビューはリンタ（C++ 1 件・QML 22 件、うち今周の行は QML 3 件だけで 2 件は `!==` を `!=` と読む誤検知、残る 1 件のアロー関数の引数名 `id` を `actionId` へ変更）＋スレッド安全性・所有権のエージェント 1 体（確信度 80 以上の指摘 0 件）。テスト 5 件（C++）+ 4 ケース（QML）追加、ctest 730 件すべて通過・警告 0 |
| 2026-08-19 | テキストプレビューの対象拡張子を広げる | M | `evolve/049` | **実機確認が残っている**（`drive` は事前判定 `flag` で実行可だったが、QML を触っていない＝起動クラッシュ／白画面の余地が無いため GUI 確認も `drive` も行っていない）: ① プレビューペインを開いて `.kt` / `.swift` / `.gradle` / `.ipynb` / `.srt` / `.patch` などの新規対象を選ぶと**中身がテキストとして出る**か ② 50KB 超のそれらが今までどおり「大きすぎます」で、通信せずに即答するか ③ 拡張子だけテキストで中身がバイナリのファイルが「プレビューできません」に倒れるか ④ `.txt` / `.json` / `.cpp` など従来の対象と画像・動画・PDF に**影響が出ていない**か ⑤ Shift_JIS の `.csv` などが今までどおり化けずに出るか。**行が求めた 3 つのうち「50KB 未満をダウンロード」「バイナリなら『プレビューできません』」は Phase 15 が既に実装済みで、残っていたのは拡張子リストだけだった**ので、`PreviewKind.cpp` の `textExtensions()` を 44 → 約 150 に広げる 1 ファイルの案を採用（理由: 判定は拡張子だけで行う設計が既にあり、そこに乗せるのが最も局所的。詳細はコミット本文）。**`ts` は media 側に残し text 側から外した**（理由: MPEG-TS は実際にサーバ側プレビューを持つが TypeScript は持たないので、media 優先の現状のほうが得。dead entry になるため text 側から削り、再追加を止めるコメントを置いた）。**拡張子の無い `README` / `Makefile` / `Dockerfile` を拾う案は入れていない**（行が「拡張子」を指しており、既存テストが `README` → `None` を明示しているため。欲しければ受信箱へ）。レビューはリンタのみ（指摘 0 件）——変更が集合リテラルとテストだけでロジック・非同期・所有権が動かないためエージェントは投げていない。テスト 2 件追加（ctest 720→722 件）、全部通過・警告 0 |
| 2026-08-19 | 検索フィルターの「Clear」を押した時点で検索結果を更新する | S | `evolve/048` | **実機確認 ほぼ済**（`drive` は事前判定 `flag` で実行可。Cloud Drive で漏斗 → Type=Folders → Apply で一覧が 15→24 件・チップ点灯（`.screenshots/540`→`541`）、開き直すと Folders が残り Apply が灰色（`542`）、そこで **Clear を押した瞬間に一覧が 24→15 件へ戻り、ポップアップは開いたまま・チップ消灯・Clear と Apply が両方灰色**（`543`）まで確認。起動時は `536-evolve-048b.png`）: ① ライトテーマでの見た目 ② **Clear のあとポップアップが開いたままなのが妥当か**（閉じるほうが自然だと感じるなら次周で変える）③ 検索欄に文字を入れた状態でフィルタを付けて Clear したとき、**文字による検索は残ったままフィルタだけ外れる**か ④ 適用していないフィルタを選んだだけで Clear したとき、再検索が走らない（一覧が一瞬も点滅しない）か ⑤ お気に入り／「最近」以外のどの画面でも同じか。**Clear を「保留を消す」から「消して即コミットする」へ変えた**（理由: ROADMAP の行が求めた非対称の解消そのもの。Apply と Clear が結果の動くタイミングで食い違わなくなる。詳細はコミット本文）。**ポップアップは閉じない案を採用**（理由: Apply は確定＝退出だが Clear は「やり直し」の起点で、続けて別の facet を選ぶ動線を潰さない）。**何も適用されていないときは push しない**（`filterActive` ガード。理由: `MegaApi::search()` は GUI スレッドを止めるので、同じ空フィルタの押し直しで再検索させない）。**ボタンの活性条件は `pendingActive` のまま**（フォーム上に消すものがあるときだけ押せる、で正しい）。レビューはリンタ対象の `.cpp`/`.h` が 0 件（QML のみ）なので QML リンタ（指摘 16 件はすべて既存行・規約・`!==` を `!=` と読む誤検知）＋ QML バインディングのエージェント 1 体。**確信度 80 以上の指摘 2 件をどちらも今周で修正**: (1) `navController` が null（ログイン／ログアウトの遷移中）のときガードがコントロール書き換えの**後**にあり、保留だけ消えて `applied*` が残る＝**Clear が自分で無効化されるのにチップは点いたまま**という詰みが作れる（ガードを書き換えの前へ引き上げ）(2) null コントローラ時の Clear と「閉じない」ことのテストが無い（2 件追加）。QML テスト 3 ケース追加（ctest の件数は QML テスト 1 ファイル＝1 エントリなので 720 件のまま）、全部通過・警告 0 |
| 2026-08-19 | 「最近」画面ではソート機能を無効化する | S | `evolve/047` | **実機確認 一部済**（`drive` は事前判定 `flag` で実行可。Recent を開いて Name 見出しを 2 回クリックしても一覧の並びが動かないことを `.screenshots/530`→`531`／`533` で確認、続けて Recent から pin 済みフォルダへ移ると Name 昇順（▲付き）に戻ることを `.screenshots/534` で確認。起動時は `.screenshots/529-evolve-047.png` / `532-evolve-047b.png`）: ① Recent で **Name / Date modified / Size のどの見出しを押しても並びが変わらない**か、押しっぱなしにしてもハイライトが出ないか ② Recent 以外（Cloud Drive・お気に入り・ゴミ箱・検索結果）では今までどおり見出しでソートできるか ③ Recent の見出しに**▼が Date modified 側に出たまま**なのは妥当か（並びの説明として残す判断をした。押せないのに印だけ出るのが紛らわしいなら消す）④ Recent を開いたまま別タブで並び順を変えても Recent 側が動かないか ⑤ Recent 表示中に見出し上でカーソルが変わらない／反応が無いことを「壊れている」と感じないか。**規則の在り処を C++ に置き、QML はそれを引くだけにした**（`FolderNavigationController::canSort` を新設。理由: `viewKind` と同じ派生で NOTIFY も共用でき、`FileTableView` が `ViewKind.Recents` を直書きせずに済む。詳細はコミット本文）。**ソートの▼は消さずに残した**（理由: 実際の並び順を説明しているので嘘ではない。見た目の判断なので気になれば `/ui-style` 側で）。**レビュー（リンタ＋エージェント 1 体）で確信度 80 以上の欠陥 1 件を検出し今周で修正**: `openRecents()` は順序を同期的に宣言するのに `viewKind()`（＝`canSort()`）は一覧が届くまで前の画面のままなので、その隙間に届いたソート要求が `mSortOrderBeforeView` を消し、Recent の順序ごとユーザーの保存順を壊す——ガードを `!canSort() \|\| mSortOrderSetByView` に統合して塞いだ（テスト 1 件追加）。リンタの指摘 2 件はどちらも既存行（brace-init / 整数タイムアウト）で不介入。テスト 2 件追加（ctest 718→720 件）、全部通過・警告 0 |
| 2026-08-19 | 「最近」画面からフォルダを除外する | S | `evolve/046` | **実機確認 一部済**（`drive` は事前判定 `flag` で実行可。Recent を開いた絵 `.screenshots/528-evolve-046-recent.png` を撮り、720 件の一覧の見えている範囲にフォルダ行が 1 つも無いことを確認。起動時は `.screenshots/527-evolve-046.png`）: ① Recent を開いてスクロールしても**フォルダが 1 行も出ない**か（リスト表示・グリッド表示の両方）② 直近 30 日に追加されたファイルは今までどおり全部出るか（フォルダを落としただけで、中のファイルが巻き添えで消えていないこと）③ Recent の件数表示（ステータスバーの "N items"）がフォルダを除いた数になっているか ④ Recent の検索欄で絞り込んだときもフォルダが出ないか ⑤ Recent 以外の画面（Cloud Drive・お気に入り・ゴミ箱・検索結果）に**影響が出ていない**か（フォルダが今までどおり出ること）。**`MegaSearchFilter::byNodeType(TYPE_FILE)` を `listRecent` に足す SDK 層 1 行の案を採用**（理由: ROADMAP が指した「絞り込み側で落とす」そのもので、`FolderNavigationService` 側で取得後に `isFolder` を弾く案より件数・ソート・空状態がすべて SDK の返り値と一致したままになる。詳細はコミット本文）。**`IMegaClient::listRecent` の契約コメントの「Folders survive this filter」を「Files only」に直した**（`evolve/039` が書いた行で、この周で偽になったため）。レビューはリンタのみ（`.cpp`/`.h` 2 ファイル、指摘 1 件はすべて既存行 = `MegaSdkClient.cpp:106` の `std::chrono::duration::count()` を `.size()` と読む誤検知）。変更が SDK アダプタ 1 行で、ロジックも非同期も所有権も動かないためエージェントは投げていない。`MegaSdkClient` は単体テストの対象外（モックが `IMegaClient` を差し替える層）なのでテストの増減なし、ctest 718 件すべて通過・警告 0 |
| 2026-08-18 | 検索フィルターを Apply で明示的に適用する | M | `evolve/045` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/525-evolve-045.png` / `526-evolve-045b.png` のみ。**ポップアップを開いた絵は撮れていない**が、保留→Apply→破棄の状態遷移は QML テスト 7 ケースで通っている）: ① 漏斗ボタンを開いて種別／カテゴリ／日時／お気に入りを変えても**その場では再検索されない**か（一覧が動かないこと）② 下段に「Clear filters」と強調表示の「Apply」が 2 つ並び、**幅 200px のコンボの下で窮屈になっていないか**（ライトテーマも）③ Apply を押すと再検索が走り、**ポップアップが閉じる**か ④ 何も変えずに開き直すと Apply が灰色（`dirty` が偽）か ⑤ 変更したあとポップアップの外をクリック／Esc で閉じ、開き直すと**いま効いているフィルタの状態に戻っている**か（捨てた選択が残らないこと）⑥ 漏斗ボタンのドットが**Apply 済みのフィルタにだけ**点くか（選んだだけでは点かないこと）⑦ 「Clear filters」を押すとコンボが Any に戻るだけで**その場では再検索されない**か、続けて Apply でフィルタが外れるか（2 クリック要ることを煩わしく感じないか）⑧ 種別で Folders を選ぶとカテゴリが Any に戻り灰色になるか⑨ フォルダを移動して検索が落ちたあと、開き直したポップアップが Any に戻っているか。**Apply はポップアップを閉じる案を採用**（理由: ポップアップが結果の一覧に被さるので、押した結果が見えないままになる。出口が Apply＝確定／それ以外＝破棄の 2 つだけになる）。**「Clear filters」は選択を消すだけで push しない案を採用**（理由: ここだけ即時適用にすると「Apply を押すまで何も起きない」という規則が 1 箇所だけ破れる）。**Apply は変更が無いあいだ無効**（理由: `MegaApi::search()` は GUI スレッドを止めるので、同じフィルタの押し直しで再検索させない）。**適用済みの値はポップアップ側に `applied*` として持つ**（理由: C++ から現在のフィルタを読む口が無く、追加すると項目の範囲を出る。そのぶんタブ切替で古くなる件は ROADMAP へ起票）。レビューはリンタ対象の `.cpp`/`.h` が 0 件（QML のみ）なので QML リンタ（指摘 16 件はすべて既存行・規約・`!==` を `!=` と読む誤検知）＋ QML バインディングのエージェント 1 体。**確信度 80 以上の指摘 3 件のうち 2 件を今周で修正**: (1) テストが `currentIndex` の代入だけで`activated`/`toggled` を出しておらず、即時 push を戻されても気づけない（シグナルを出す形に変更）(2) `navController` が null（ログイン／ログアウトの遷移中）でも Apply が適用済みを名乗る（ガードを追加＋テスト 1 件）。残る 1 件（タブ切替で `applied*` が古くなる）は**この周より前からある**ので ROADMAP「優先度: 中」の末尾へ起票。QML テスト 7 ケース追加（ctest の件数は QML テスト 1 ファイル＝1 エントリなので 717→718 件）、全部通過・警告 0 |
| 2026-08-18 | 共有リンク削除時に確認ダイアログを表示する | S | `evolve/044` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/524-evolve-044.png` のみ。**メニューもダイアログも開いた絵は撮れていない**が、ダイアログの生成・`confirm()`・`accept()` / `reject()` は QML テスト 3 ケースで通っている）: ① ファイル／フォルダを 1 つ右クリックして「Remove link」を押すと確認ダイアログが出るか（押した瞬間にリンクが消えないこと）② 文面 "Remove the link to "〜"? Anyone holding it will lose access." が妥当か、長いファイル名でも窓の外へはみ出さないか ③ Yes でリンクが実際に消えるか（消す前にコピーした URL が無効になること）④ Cancel／Esc／窓の外クリックでリンクが**残る**か ⑤ ダイアログが出ている間に他タブのアップロード等で一覧が更新されても、**プロンプトが名指しした項目**のリンクが消えるか（選択が変わっても勝手に別物を消さないこと）⑥ リンクを持たない項目で Yes を押しても失敗トーストが出ないか（`evolve/040` の「リンク無しへの `disableExport` は SDK を呼ばずに成功」がそのまま効くはず）⑦ ライトテーマでの見た目。**`ConfirmRubbishDialog` / `ConfirmPermanentDeleteDialog` と同じ「ビューが所有する確認ダイアログ＋`request*()` コールバック」の形をそのまま写した**（理由: `ActionCatalog` は singleton でビュー固有の Item に届かないため、既存の破壊的操作 2 つが通っている唯一の経路。詳細はコミット本文）。**単一対象なので `handles`（配列）ではなく `handle` 1 本を採る形にした**（理由: `MenuActionResolver` の `RemoveLink` は `SingleOnly` で、複数選択では出ない）。**新しい文言・アイコンは足していない**（メニュー項目名は「Remove link」のまま）。レビューはリンタ対象の `.cpp`/`.h` が 0 件（QML のみ）、QML のバインディング・シグナル配線のエージェント 1 体。QML テスト 3 ケース追加（ctest の件数は QML テストが 1 ターゲット扱いなので 717 件のまま）、全部通過・警告 0。**この周は `--reconfigure` で vcpkg が依存ライブラリ一式（openssl / freeimage / pdfium / ffmpeg ほか）をソースから作り直し、configure だけで 3185 秒かかった**——原因未特定、次周も同じなら要調査 |
| 2026-08-18 | ファイルのダブルクリックでダウンロードしない | S | `evolve/043` | **実機確認が残っている**（`drive` は事前判定 `attended` だったので実行を試みたが**許可が下りなかった**ため未使用。GUI 確認は起動＋スクショ `.screenshots/523-evolve-043.png` のみで、Cloud Drive の一覧が通常どおり出ることまで）: ① **ファイルをダブルクリックしても何も起こらない**か（リスト表示・グリッド表示の両方。ダウンロードが始まらず、ステータスバーにも何も出ないこと）② フォルダのダブルクリックは今までどおり開くか（同じく両表示）③ コンテキストメニューの Download と、アプリ外へのドラッグ以外のダウンロード導線が今までどおり効くか ④ ダブルクリックが完全に無反応なことを「壊れている」と感じないか（何のフィードバックも出さない判断をしたので、ここが唯一の賭け。プレビューを開く等の代替は ROADMAP の行が求めていないので入れていない）。**`TabContentPane.activate()` の else 枝を落とすだけの局所案を採用**（理由: ROADMAP が指した唯一の合流点で、両ビューの `activateRequested` シグナルと `onDoubleTapped` は触らずに済む。シグナルの `name` / `sizeBytes` は未使用になるが、削ると 3 ファイルに広がるうえ、将来ダブルクリックへ別の動作を割り当てるときに戻す必要が出るので残した）。**`CLAUDE.md` の製品説明の「double-click-to-open」を「double-click-to-open folders」に直した**（ユーザー向けの機能が減ったため）。レビューはリンタ対象の `.cpp`/`.h` が 0 件（QML 3 行の削除のみ）なのでエージェントも投げていない。テストの増減なし、ctest 717 件すべて通過・警告 0 |
| 2026-08-18 | 「最近」画面を既定で新しい順に並べる | S | `evolve/042` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/521-evolve-042.png` / `522-evolve-042b.png` のみ。**Recent 画面を開いた絵は撮れていない**——Cloud Drive が名前昇順のまま出ることは確認済み）: ① 側面パネルの Recent を押す／中クリックで新しいタブに開くと、**更新日時の降順**（新しいものが上）で並ぶか ② そのとき列見出しの▼が **Modified** 側に付いているか（一覧と見出しが食い違っていないこと）③ Recent から Cloud Drive／お気に入り／ゴミ箱／戻るへ移ると、**Recent に入る前のソート順に戻る**か（見出しの▲▼も戻ること）④ Recent 表示中に列見出しを押して自分でソートし直したときは、その順序が**そのまま次の画面へ持ち越される**か（ユーザーが選んだものは戻さない設計）⑤ Recent を開いたあとにアプリを再起動して**新しいタブが以前どおりの既定順**か（＝画面の既定が保存値を汚していないこと。`HKCU\Software\MegaExplorer\MegaExplorer\sortColumn` が変わっていないこと）⑥ すでに Recent を見ている状態でもう一度 Recent を押したとき、④で変えたソートが**リセットされない**か。**画面ごとのソート状態を新設せず、コントローラが画面固有の順序を `mSortOrder` に一時的に押し込み、離れるときに元へ戻す案を採用**（理由: ROADMAP が挙げた 2 案のうち「`SortKey` に `CreationTime` を足して列見出しに対応づける」は列が 1 本増える＝画面 1 つ分の見た目の判断になり、「画面ごとのソート状態」は Main.qml の last-write-wins 設計を丸ごと作り替える。どちらも局所案ではない。詳細はコミット本文）。**画面の既定はウィンドウ全体の保存値へ書き戻さない**（`FileTableView` の `applyingViewSortOrder` ガード。理由: 保存値は「新しいタブの出発点」としてユーザーが選んだもので、画面が勝手に書き換えてよい場所ではない）。**すでにその画面にいるときの再クリックでは既定を適用し直さない**（理由: そこでユーザーが選んだソートを捨てないため）。**新しいタブでは `Component.onCompleted` の保存値復元と `openRecents()` の順序が QML 上保証されないので、画面が先に順序を宣言していたら復元を 1 回だけ捨て、勝った順序をエコーバックする**（`mSortOrderSetByView`）。「最近」画面ではソート機能を無効化する（優先度: 中）は**この土台の上に乗るので後回しにした**——ROADMAP の当該行にその旨を追記済み。**レビュー（リンタ ＋ エージェント 1 体）で確信度 80 以上の欠陥 2 件を検出し、どちらも今周で修正**: (1) 初回取得が失敗するとガードが張りっぱなしになり、ユーザーの列見出しクリックまで無期限に握り潰す（1 回きりに変えた）(2) 「最近」の順序がそのタブの以降のナビゲーション全部に漏れる（＝項目の範囲外）ので、離脱時に元へ戻す `restoreUserSortOrder()` を足した。リンタは今周の行に 1 件（`std::optional` の既定構築＝GCC 向けの規則なので `= std::nullopt` を明記して解消）、既存行に 2 件（brace-init / 整数タイムアウト）で不介入。テスト 3 件追加（ctest 714→717 件）、全部通過・警告 0 |
| 2026-08-17 | 設定画面 | M | `evolve/041` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/520-evolve-041.png` のみ。**More メニューもダイアログも開いた絵は撮れていない**。ただしダイアログの生成・`open()`・コンボの初期選択は QML テスト 5 ケースで通っている）: ① More（…）メニューの最上段に歯車付きの「Settings」が出て、区切り線の下に About / Licenses / Sign out が並ぶか ② **歯車（Segoe U+E713 Settings）が 16px で箱にならずに描けているか**（両フォントの cmap には在ることだけ確認済み、描画は未確認。`docs/DESIGN_IMPROVEMENT.md` §11 の但し書き）③ ダイアログに「Theme」＋コンボ（Use system setting / Light / Dark）が出て、窓の外へはみ出さないか ④ Light / Dark を選ぶと**その場で**アプリ全体（FluentWinUI3・自前キャプション行・QWindowKit のネイティブ 1px 枠）が切り替わるか ⑤ Use system setting に戻すと OS 設定に追従し直すか（Windows 側を切り替えて確認）⑥ 再起動後も選択が残るか（`HKCU\Software\MegaExplorer\MegaExplorer\colorSchemePreference`）⑦ `MEGAEXPLORER_COLOR_SCHEME=light` を立てて起動すると保存値より env が勝つか ⑧ ライトテーマでのダイアログの見た目と幅。**言語セレクタは出さず、テーマだけで出した**（理由: `.ts` も `QTranslator` も無い段階では選択肢が 1 つしか無い。ROADMAP の「多言語対応（日本語）」行に置き場のメモを追記済み）。**新しい C++ コントローラは作らず、既存の QtCore `Settings`（Main.qml の property alias 群）＋ QML からの `Application.styleHints.colorScheme` 代入で実装**（理由: 局所案。`colorScheme` は Qt 6.8 以降 writable で、`Qt.Unknown` 代入が「OS に追従」に戻す経路であることは実測で確認）。**保存値は Qt::ColorScheme の生値（0/1/2）**（理由: 変換表を持たずに済む）。**起動時の適用は main.cpp、変更時の適用は Main.qml に分けた**（理由: env の `MEGAEXPLORER_COLOR_SCHEME` を起動時に勝たせ続けるため。QML から env は見えない）。レビュー（リンタ ＋ QML バインディングのエージェント 1 体）は**確信度 80 以上の指摘 1 件を「設計上の許容」として不採用**: env override 中はダイアログが「保存値」を出すので実際の配色と食い違う——env はスタイル調整用の開発者向け経路（`CLAUDE.md`）で、そのために context property を 1 本増やす価値が無いと判断した。リンタの 1 件は既存行（`main.cpp:45` の include 順）。QML テスト 5 ケース追加、ctest 714 件すべて通過・警告 0 |
| 2026-08-17 | リンクの共有 | M | `evolve/040` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/519-evolve-040.png` のみ。**メニューを開いた絵は撮れていない**）: ① ファイル／フォルダを 1 つ右クリックすると「Copy link」「Remove link」が Favourites の下に並ぶか ② 「Copy link」でトースト "Link copied to clipboard" が出て、貼り付けた URL がブラウザで開けるか ③ 同じ項目でもう一度押すと**同じ** URL が返るか（新しいリンクが増えないこと）④ 「Remove link」の後にその URL が無効になるか ⑤ リンクの無い項目で「Remove link」を押しても失敗トーストが出ないか ⑥ **鎖アイコン（Segoe の U+E71B Link） が 16px で箱にならずに描けているか**（両フォントの cmap には在ることだけ確認済み、描画は未確認。`docs/DESIGN_IMPROVEMENT.md` §11 の但し書き）⑦ 複数選択では 2 つとも消えるか。**ダイアログではなく「クリップボードへコピー」1 発を採用**（理由: 新しいダイアログは見た目の判断が要りループのスコープ外。理由の詳細はコミット本文）。**共有済みかどうかは持たない**（`FileEntry` に `isExported` を足すと行マーカー＋その場書き換え＋他タブへのファンアウトまで芋づるになるため、ROADMAP へ「一覧で『リンクを共有済み』が分かるようにする」として起票）。**リンク無しへの `disableExport` は SDK を呼ばずに成功を返す**（`MegaNode::isExported()` で見て、無操作に失敗トーストを出さないため）。**発行するのは期限なし・読み取り専用のリンクのみ**（writable/megaHosted は鍵を持ち回る必要があり、UI がそれを置く場所を持たない）。**クリップボード書き込みは `qobject_cast<QGuiApplication*>` ガードの内側**——単体テストの実行体は `QCoreApplication` で、`QClipboard` を作る土台が無い |
| 2026-08-17 | 最近追加された項目のビュー | M | `evolve/039` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/518-evolve-039.png` のみ。**側面パネルに Recent 行が出て時計の字形が正しく描かれることまでは確認済み**だが、行を押して一覧を開けていない）: ① Recent 行を押すと直近 30 日に**追加**された項目が並ぶか（古いファイルを今日アップロードしただけのものも出ること——mtime ではなく ctime で絞っているのがこの周の判断）② **フォルダも出る**か（`byCreationTime` は `byModificationTime` と違いフォルダを落とさない）③ ゴミ箱の中身や共有フォルダが**混ざらない**か（Cloud Drive ルートに `byLocationHandle` している）④ 一覧が**名前昇順で出る**ことを許容できるか（ROADMAP「中」へ別項目として起票済み。日付列を押せば新しい順になる）⑤ 検索欄に打つと Recent の中だけを絞り込むか、フィルタボタンが**消えている**か ⑥ 行の右クリックで Cut / ゴミ箱へ / 新しいフォルダ / 貼り付けが**出ない**か、Download / Copy / Rename / Go to folder は出るか（お気に入り画面と同じ）⑦ Recent の行を掴んでドラッグすると**移動が拒否**され、Ctrl を押したコピーだけ通るか ⑧ 中クリックで別タブに開くか、タブ名とパンくずが「Recent」になるか ⑨ 30 日に何も無いアカウントで空状態の文言が出るか、その下の手引きの 1 行が**出ない**か。**API は調査の 3 系統のうち `search` + `MegaSearchFilter` を採用**（理由: `getRecentActionsAsync` はバケット列を返し `vector<FileEntry>` ですらなく、日付見出し付きの専用ビューが要る——`STUDY_SPECIAL_VIEWS_FRAMEWORK.md` 結論 5 そのもので、1 周に収まらない）。**絞り込みは `byModificationTime` ではなく `byCreationTime`**（理由: 項目名が「最近**追加**された」で、アップロードはローカルの mtime をそのまま運ぶため、mtime だと古いファイルを今日上げても出ない。さらに mtime フィルタは SDK 仕様でフォルダを落とす）。**窓は 30 日固定**（理由: SDK 自身が `getRecentActionsAsync` に推奨している値。設定項目にしない）。**ソート順はお気に入り画面と同じくタブの列ソートをそのまま流す**（理由: ソートはウィンドウ全体で 1 つで、`FileTableView` の `Component.onCompleted` がタブ生成のたびに流し込むので、画面固有の既定を入れても上書きされる。既存のソート設計を触るのはこの項目の範囲外なので、ROADMAP「中」へ別項目として起票）。**メニューの可否・D&D の拒否・空状態・フィルタボタンはお気に入り画面をそのまま写す**（理由: どちらも宛先フォルダを持たない平らな横断クエリで、規則を 2 つに分ける理由が無い）。**側面パネルでは Recent を最上行に置いた**（理由: ゴミ箱を最下に残すため。既存 2 行の順番は変えていない）。`goBack` と `refreshCurrent` に同じ画面種別の if 連なりが 2 本できるので、private な `fetchListing()` に集約した。レビュー（リンタ ＋ エージェント 2 体 = C++ の所有権・寿命＋リファクタの等価性 / QML のバインディング）で**確信度 80 以上の欠陥は 0 件**。基準下の指摘 1 件（`IMegaClient.h` のスレッド契約コメントの「同期に返るメソッド」一覧に `listRecent` が無い）は今周修正し、ついでに既存の漏れ `getRubbishChildren` も足した。リンタは今周の行に指摘 1 件だが誤検知（`std::chrono::duration::count()` を「`.size()` にしろ」）、既存行に 3 件。テスト 7 件追加（ctest 695→702 件）、全部通過・警告 0 |
| 2026-08-17 | 詳細検索 — フィルタのポップアップ（種別・カテゴリ・日時・お気に入り） | M | `evolve/038` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/516-evolve-038.png` のみ。**ボタンが出てファンネルの字形が正しく描かれることまでは確認済み**だが、ポップアップの中身は開けていない）: ① 検索欄の右のファンネルボタンを押すとポップアップが出て、Type / Category / Created / Favourites only / Clear filters の 5 つが並ぶか、ツールバーの外へはみ出せているか（`Popup.Window`）② **何も打たずにカテゴリだけ選ぶと検索が走る**か（この項目の肝。空文字＋フィルタで `MegaApi::search` を叩く）③ フィルタが効いている間、ボタンの右上にアクセント色の丸いチップが出るか、`Clear filters` で消えるか ④ **Type を Folders にすると Category が灰色になり、選択済みだった値が Any に戻る**か（カテゴリを付けると SDK がファイルしか返さないため）⑤ 日時が「Past 24 hours / 7 days / 30 days / year」の**相対窓**として妥当か（カレンダー日ではない）⑥ フォルダを開く・戻る・お気に入りへ行くとフィルタが落ち、**ポップアップの表示も既定に戻る**か（レビューで直した箇所そのもの）⑦ **お気に入り画面ではボタンごと消える**か ⑧ ライトテーマでのポップアップの配色（`surfaceAlt` + `stroke`）⑨ カテゴリ各種（Images/Audio/Video/Documents/PDFs/Presentations/Spreadsheets/Archives/Programs/Other）が実際に期待どおり絞るか。**フィルタは `IMegaClient::search` にだけ通す案を採用**（理由: お気に入り画面が使う `listFavourites` にも足すと、モックの引数が 1 つ増えて既存テスト約 20 箇所の機械的な追随が要り、差分が項目と無関係に膨らむ。効かない画面ではボタンを隠し、`ROADMAP`「優先度: 中」へ別項目として起票した）。**選択状態は QML 側に置く案を採用**（理由: 検索欄のテキストと同じ扱い。無効化する唯一の事象＝ナビゲーションは既存の `searchCleared` で届く）。**日時はカレンダー日ではなく相対窓**（理由: `MegaSearchFilter::byCreationTime` はタイムスタンプ 2 つを取り、「今日の 0 時」はタイムゾーンが要る。`src/core` は Qt も SDK も見ない層）。**enum は QML 用ミラーを 3 クラスに分けた**（理由: QML は型の enumerator を型スコープに平坦化するので、1 クラスだと 3 つの `Any` が衝突する）。**レビュー（リンタ + エージェント 2 体）で 1 件の欠陥を検出し今周で修正**: ComboBox / CheckBox は選択時に自分で `currentIndex` / `checked` を書くため、それらに張った `currentIndex: root.nodeType` 形のバインディングが最初の操作で切れ、`reset()` がフィルタだけ消して**表示は古い選択のまま残る**（⑥ が壊れる）。ミラーのプロパティを廃してコントロール自身を唯一の状態にし、`filterActive` / `apply()` はそこから読む形へ作り直した。C++ 側のエージェントは指摘 0、リンタも今周の行に指摘 0（既存行に 1 件、`SortOrder mSortOrder{...}` の brace-init＝誤検知）。テスト 5 件追加（ctest 690→695 件）、全部通過・警告 0 |
| 2026-08-17 | 衝突確認ダイアログの説明文をボタンのツールチップへ移す | S | `evolve/037` | **実機確認が残っている**（`drive` は事前判定 `skip`＝無人なので使わず、GUI 確認は起動＋スクショ `.screenshots/515-evolve-037.png` のみ。衝突ダイアログはドロップ／貼り付けからしか出ないため中身は未確認）: ① コピー・移動のダイアログ（Ctrl+C→Ctrl+V／Ctrl+ドロップ）で **Continue / Rename にマウスを乗せると 0.5 秒後に説明のツールチップが出る**か、360px で折り返されて窓の外へはみ出さないか ② アップロードのダイアログの **Replace** も同様か ③ フォルダが絡む衝突では **Skip** にもツールチップが出て、ファイルだけの衝突では Skip に**何も出ない**か（空の枠が出たらバグ）④ 版管理を**オフ**にしたアカウントでは「cannot be recovered」の 1 文が**本文に残り**、Rename が既定のままか ⑤ 本文が短くなったぶんダイアログが小さくなって、見出し＋ファイル名＋「The other N ...」だけになっているか ⑥ ボタンが `DialogButtonBox` に取り込まれた状態でツールチップの位置がボタンの真上に出るか（`ToolTip` を Button の子として宣言しているため、`parent` の解決が想定どおりか）⑦ **キーボードだけで操作したときに説明が読めない**（ホバー限定にした）ことが許容できるか。**破壊的な文面だけ本文に残す案を採用**（理由: 版管理オフの「outright / cannot be recovered」はホバーしないと見えない時点で警告として機能しない。ここだけ従来どおり本文に出す）。**属性 `ToolTip.text` ではなく明示的な `ToolTip` 子要素を採用**（理由: 属性版は 1 個の共有インスタンスなので、折り返しに要る `implicitWidth` の上限を書くとアプリ中の全ツールチップに波及する。既存の `AddressToolBar` / `StatusBar` は単語 1 つなので上限が要らない）。**Skip のツールチップはフォルダが絡むときだけ**（理由: ファイルだけなら Skip に説明することが無く、空文字のツールチップは枠だけの箱になる）。レビューはリンタ対象の `.cpp`/`.h` が 0 件（QML のみの周）＋ QML バインディング／レイアウトのエージェント 1 体。QML テスト実行時に binding loop・`ReferenceError` の類は 0。QML テスト関数 325→327、ctest 690 件すべて通過・警告 0 |
| 2026-08-17 | 同名のファイル・フォルダへのリネームを禁止する | S | `evolve/036` | **実機確認 一部済**（`drive` は事前判定 `flag`＝許可あり。Cloud Drive 直下のフォルダ `100` を F2 で既存フォルダ名 `500` に改名しようとするとトースト「That name is already used here — the item was not renamed」が出て行は `100` のまま＝`.screenshots/512-rename-taken.png`。続けて `100`→`100x`→`100` が通ることも確認し、テストアカウントは現状復帰済み）。**残る確認**: ① 同名の**ファイル**へのリネーム、および「ファイル名をフォルダ名に」「フォルダ名をファイル名に」の交差（今回はフォルダ同士のみ確認。MEGA は種別違いの同名を許すが、このアプリは両方拒否する実装）② **検索結果**と**お気に入り**の行を F2 で改名したとき、判定が表示中の一覧ではなく**実際の親フォルダ**の兄弟を見ているか（`IMegaClient::siblingNameTaken` を足した理由そのもの）③ 大文字小文字だけを変えるリネーム（`a.txt`→`A.txt`）が通るか（自ノードは衝突に数えない実装だが、SDK の `getChildNodeOfType` が大小無視で引くなら別の兄弟に化ける可能性がある）④ ライトテーマでのトーストの見た目。**判定を「表示中の一覧」ではなくポート越しの同期問い合わせにする案を採用**（理由: リネームは `ViewKind::CloudDrive` と `Favourites` の両方で出せ、検索結果も同じ一覧に混ざる。キャッシュの兄弟で判定すると別フォルダの同名行で誤って拒否し、逆に本当の兄弟を見落とす）。**警告はインライン検証ではなく確定時のトースト＋キャンセルにする案を採用**（理由: ROADMAP の指定どおりで、既存の `renameInvalidName` と同じ経路に乗る）。**判定自体が失敗したらリネームを通す案を採用**（理由: 壊れた問い合わせは名前について何も言っておらず、拒否すると直しようのない理由でリネーム不能になる）。**refuse は `FileOperationService::rename` に置き `kEExist` で返す**（`isValidName` の `kEArgs` と同じ層・同じ形。コントローラは新トースト `renameNameTaken` に振り分けるだけ）。レビューはリンタ + エージェント 1 体（所有権・寿命・スレッド）で**指摘 0**（リンタの 1 件は既存行の `TMO-1`）。テスト 4 件追加、ctest 690 件すべて通過・警告 0 |
| 2026-08-17 | 名前衝突（アップロード）4／同一操作内の同名を衝突として数える | S | `evolve/035` | **実機確認が残っている**（QML を 1 行も触っていない＝見た目は不変なので GUI 確認なし。`drive` は事前判定 `flag`＝許可ありだったが、OS のドラッグ&ドロップを合成できないため使わず）: ① 別フォルダから掴んだ同名ファイル 2 件を、**宛先に同名が無い**フォルダへ 1 回でドロップすると、アップロードの衝突ダイアログが出るか（以前は訊かれず両方上がって MEGA 上で版が積まれていた）② そのダイアログが重複を **1 件**として数えるか（2 件目だけが衝突。1 件目は無傷側の N に入る）③ **スキップ**を選ぶと 1 件目だけが上がるか（0 件になったらバグ）④ 同名でも**種別が違う**（`x` というファイルと `x` というフォルダ）2 件は今までどおり訊かれないか ⑤ ネストした階層でも同じか（`a/dir/p.jpg` と `b/dir/p.jpg` を宛先の既存 `dir` へ）⑥ 「このまま実行」で上げると 2 件が版として積まれ、**どちらが最新版になるか**（並列発行なのでサーバ到着順まかせ＝非決定的。CM §6-1 と同じ論点で、結果を `docs/REQUESTS.md` へ）。**先頭が生き残る規則を採用**（理由: コピー・移動側の `FileMutationController::collidingEntries()` と同じにした。両経路で違う規則にすると「どちらが残るか」の説明が 2 つになる）。**フォルダ同士の同名は対象外にする判断**（理由: 宛先に同名フォルダが無いと降りる先のハンドルが無く、「まだ存在しないフォルダ」をローカル同士で突き合わせる新しい形が要る。局所案に収まらないので「優先度: 中」へ別項目として起票）。**レビュー（リンタ + エージェント 1 体）で 1 件の欠陥を検出し今周で修正**: 重複パスの除去を**呼び出し側の綴り**で行っていたため、同じファイルの 2 通りの綴り（QML のドロップは `QUrl::toLocalFile()` のスラッシュ、下流はバックスラッシュ）が両方生き残り、2 つ目が自分の 1 つ目と衝突扱いになって**スキップ計画から両方落ちる**。下流と同じ「解決後のパス」で除去する形に直し、テストを 1 件追加。リンタは指摘 0。テスト 5 件追加（ctest 686→687 件）、全部通過・警告 0 |
| 2026-08-17 | 名前衝突（アップロード）3／衝突ダイアログに件数と追加サイズを添える | S | `evolve/034` | **実機確認が残っている**（`drive` は事前判定 `flag`＝許可ありだったが、衝突ダイアログを出すまでの合成（OS のドラッグ&ドロップ、または Ctrl+C→Ctrl+V→モーダル）が長く命中率が低いので使わず、GUI 確認は起動＋スクショ `.screenshots/510-evolve-034.png` のみ）: ① 同名ファイルのあるフォルダへドロップすると、アップロードのダイアログの見出しが `3 file(s) ... in the destination (12.4 MB):` の形になり、**バイト数が実際のローカルファイルの合計と合う**か ② 「The other N file(s) are uploaded either way (X)」の X が残り側の合計と合うか（衝突側と足すと全体になるはず）③ 0 バイトのファイルだけを衝突させると括弧ごと消えるか（`(0 B)` と出たらバグ）④ **コピー**（Ctrl+C→Ctrl+V / Ctrl+ドロップ）のダイアログにも同じ 2 か所にサイズが出るか。とくに**フォルダの衝突**でサブツリー全体の合計になっているか（`MegaApi::getSize` は再帰、MEGA の Web のフォルダサイズと突き合わせる）⑤ **移動**のダイアログにはサイズが 1 つも出ないか（出たら判断ミス）⑥ フォルダが絡む衝突で「"Skip" leaves those folders out entirely, with everything inside them.」の 1 行が出るか ⑦ 数百件の貼り付けでダイアログが出るまでに体感の間が空かないか（サイズ計算は同期・GUI スレッド）。**「追加サイズ」はコピーとアップロードにだけ添える案を採用**（理由: 移動はアカウント内の付け替えでバイト数を足さないので、数字を出すと「転送が起きる」と読めてしまう。`moveNameConflict` には引数自体を足していない）。**再帰的なファイル件数は添えない判断**（理由: ROADMAP のメモが挙げていた `MegaApi::getNumChildFiles()` は **直下しか数えない**と SDK ヘッダに明記されており、再帰版の `getFolderInfo` はリクエスト＝同期に返らない。仕様 UP §1-6 / CM §1-6 の「getNumChildFiles も同様」は事実誤り。件数は既に出ている項目数・ファイル数で足りるので、サイズだけにした）。**サイズは C++ 側で整形して文字列で渡す案を採用**（理由: QML に `formattedDataSize` 相当が無い。既存の `AccountController::storageText` と同じ 1024 進・`DataSizeTraditionalFormat`。0 は空文字で渡し、ダイアログが括弧ごと落とす）。**アップロード側は SDK を使わずローカルで数える**（`expandedFileCount` を件数＋バイト数を返す `expandedVolume` に置き換え。衝突ファイルは `QFileInfo` で引く）。**コピー側は `IMegaClient::subtreeSize()` を新設**（`MegaApi::getSize` の同期ラッパ。`checkMove` と同じ「ネットワークに行かない読み取り」の族）。**レビュー（リンタ + エージェント 1 体）で 1 件の欠陥を検出し今周で修正**: サイズ集計を `startCopyBatch` のループで無条件に回していたため、**衝突が 1 件も無い普通の貼り付け**でも全エントリのサブツリー走査を GUI スレッドで払い、結果は捨てていた。衝突が 1 件以上あるときだけ数える形にし、「衝突ゼロなら `subtreeSize` を 1 回も呼ばない」テストを追加。リンタは今周の行に指摘 0（既存行に 1 件、`std::optional` のメンバ既定初期化＝誤検知）。テスト 3 件 + QML 5 件追加（ctest 679→682 件、QML のテスト関数は 320→325）、全部通過・警告 0 |
| 2026-08-17 | 名前衝突（アップロード）2／衝突ダイアログをネスト込みの 1 枚に畳む | M | `evolve/033` | **実機確認が残っている**（`drive` は事前判定 `flag`＝許可ありだったが、ドロップは OS のドラッグ&ドロップで `drive` に合成手段が無いので使わず、GUI 確認は起動＋スクショ `.screenshots/509-evolve-033.png` のみ）: ① **100 個入りの `/dir` のローカルコピーに `file1.txt` を 1 つ足してドロップすると、ダイアログが出ずに 101 個になる**か（この一本化の狙いそのもの）② ネストした重複が `dir/sub/a.png` の形で列挙されるか、また即座に出るか（走査がネットワークに行っていない証拠）③ **スキップ**を選ぶと、重複ファイルだけが上がらず、衝突を含まないサブフォルダは 1 本のフォルダ転送で丸ごと上がるか（MEGA の Web で中身と版数を確認）④ 本文の「The other N file(s) are uploaded either way.」の N が実際の残数と合うか ⑤ 版管理を**オフ**にしたアカウントで本文が「cannot be recovered」に変わるか ⑥ 隠しファイル・非 ASCII 名・フォルダへの `.lnk` を含むドロップで数が合うか ⑦ 100 件上限に当たるドロップで、上限拒否のほうが先に出るか。**走査結果は `UploadScanService` を注入して受ける案を採用**（理由: ROADMAP のメモは `ILocalFileSystem` の注入だったが、`UploadController` は `IMegaClient` を持っておらず、走査サービスは両ポートを既に持っている。コントローラに 2 本目のポートを生やさずに済む）。**スキップの計画づくりも `src/core` 側（`planSkippingCollisions()`）に置く案を採用**（理由: 各階層の MEGA ハンドルは走査しか知らず、out パラメータで外へ出すより、同じ 2 ポートを持つサービスで完結させるほうが局所的。Qt 非依存なので単体テストも素直）。**スキップの走査が失敗したら何も上げずにトーストを出す案を採用**（理由: ダイアログを出すための走査は失敗しても「訊かずに上げる」で済むが、スキップで同じことをするとユーザーが助けたばかりのファイルに版を積む。非対称にした）。**レビュー（リンタ + エージェント 1 体）で 1 件の欠陥を検出し今周で修正**: 同じ葉名を持つローカルパスが 2 つあると `scanLevel` の重複排除が 2 つ目を**エントリごと**捨てていたため、衝突行が 1 つしか出ず、スキップの計画がパスで引く以上 2 つ目は上がってしまう（フォルダ版はさらに悪く、降りなかった同名フォルダが SDK に丸ごと渡って再帰マージされる）。問い合わせる名前だけを重複排除する形に直し、テストを 3 件追加。**別件 1 件を ROADMAP（中）へ**: `ILocalFileSystem::listDirectory()` に失敗の伝え方が無く、読めないディレクトリが空と同じに見える。リンタは今周の行に指摘 0（既存行に 2 件、`main.cpp` のインクルード順と `std::optional` のメンバ既定初期化＝どちらも誤検知寄り）。テスト 15 件追加（ctest 664→679 件）、全部通過・警告 0 |
| 2026-08-17 | 名前衝突（コピー・移動）／同一操作内の同名を衝突として数える | S | `evolve/032` | **実機確認**: ① 別フォルダ由来の同名ファイル 2 件を空のフォルダへ貼り付け／ドロップすると、宛先に既存が無くてもダイアログが出るか（出るのが正しい。以前は移動＝同名が 2 つ並ぶ、コピー＝2 件目が黙って `a - Copy.txt` になっていた）② そのダイアログで「このまま実行」を選んだときのコピー結果——**2 件が版として積み上がり一覧に 1 行しか出ない**はずで、CM §6-1 が「文面を足すか 2 件目を自動リネームするか実測してから決める」と残している論点。**レビューで §6-1 に無い側面が出た**: 2 件は同名・空 `newName` で並列発行されるので、どちらが最新版になるかはサーバ到着順まかせ＝非決定的。見え方だけでなく「どちらの中身が残るか」も確かめて `docs/REQUESTS.md` へ ③ 同名でも種別が違う（`a` というファイルと `a` というフォルダ）2 件は今までどおり訊かれないか。**両経路 1 項目だったのをコピー・移動側だけ実装する判断**（理由: アップロードのダイアログはまだ Replace / Skip の 2 択で、宛先に既存が無い同名 2 件に Skip を選ぶと両方失う。「アップロード 2」がそのダイアログを作り直すので、その後に回した——ROADMAP の行は「アップロード 4」として残してある）。**ダイアログの文面は変えない判断**（理由: 「宛先に既に存在する」は結果としては正しく、5 文の場合分けを触ると項目の範囲を出る）。**判定は 1 エントリ 1 フラグの `collidingEntries()` に集約**（理由: 予告・事前予約・計画の 3 ループが同じ判定を見ないと、ダイアログが予告した名前と実際に付く名前がずれる） |
| 2026-08-16 | 名前衝突（アップロード）1／ローカル再帰走査のポートと、衝突枝だけ降りる走査 | M | `evolve/031` | **実機確認は不要に近い**（UI に一切配線していない＝アプリの挙動は不変。`drive` は無人判定 `skip`、QML も触っていないので GUI 確認なし）。**残る確認は次の「アップロード 2」で配線したあと**: ① ネストした重複が実物の MEGA で数えられるか（`getChildNodeOfType` がサーバに行かない＝ダイアログが即座に出るか）② 隠しファイル・非 ASCII 名・`.lnk` を含むフォルダで数が合うか。**ポートは「1 階層ぶんの列挙 + 1 件の stat」に絞る案を採用**（理由: 走査コストを衝突面の大きさに比例させるため。UP §5-1）。**同名フォルダの照合用に `IMegaClient::findChildFolders` を新設**（`findChildFiles` の TYPE_FOLDER 版。SDK 側は共通の private ヘルパに寄せた。走査は「両側に同名フォルダがある枝」にしか降りないので、フォルダのハンドルが要る）。**深さ上限 32 に当たったら成功ではなく失敗を返す案を採用**（理由: ジャンクションの循環で打ち切った走査は「衝突なし」を証明していないため。`kEInternal`）。**MEGA 側は同名の兄弟を許すので、問い合わせる名前は重複排除し、答えは名前で引き戻す**（同一操作内の同名の扱いそのものは別項目のまま）。**レビュー（リンタ + エージェント 2 体）で 6 件検出**: 走査側 3 件（同名兄弟で 1 ファイルが 2 行になる／深さ打ち切りが「衝突なし」に見える／`parentIsRoot` が一度も true でテストされていない）と アダプタ側 3 件（Qt が `.lnk` を指し先として報告するのでフォルダへのショートカットが枝として歩かれる／`QDir::System` 無しでリンク切れが列挙から落ちる／隠しファイルと非 ASCII 名のテストが無い）を**すべて今周で修正**。リンタは指摘 0。テスト 14 件追加（ctest 650→664 件）、全部通過・警告 0 |
| 2026-08-16 | 名前衝突（コピー・移動）3／衝突ダイアログの本文と既定ボタン | S | `evolve/030` | **実機確認 一部済**（`drive` は事前判定 `attended` だったので実行した。`megatool` で `/MegaExplorerFixture/cA`・`cB` に同名 `dupe.txt` を作り、cA→cB の Ctrl+C→Ctrl+V で**衝突ダイアログを実際に開いて撮影**＝`.screenshots/507-drive-conflict.png`。版管理**有効**側の Continue にアクセント＋フォーカスが乗り、本文が「keeps the existing files as earlier versions」になることを確認。フィクスチャは削除済み・ゴミ箱にはある）。**残るのは版管理を切ったときだけ**: ① MEGA の Web 設定でファイルのバージョン管理をオフにし、同じ手順でコピーすると本文が「deletes the existing files outright … cannot be recovered」に変わり、**既定ボタンが Rename** になるか ② その状態で Enter が Rename を撃つか ③ ファイルとフォルダが混在した衝突の本文が読めるか ④ **ライトテーマ**でもアクセントが既定だと分かるか ⑤ 版管理オフのままログアウト→別アカウントでログインすると既定が Continue に戻るか。**版管理フラグはログイン時に 1 回だけ読む案を採用**（理由: ダイアログは開いた瞬間に文面と既定を確定させる必要があり、待てる往復が無い。`AccountController` は本来 lazy だがここだけ例外にし、配線は `main.cpp` の `AuthController::LoggedIn`）。**ダイアログは `accountController` を直読みせず `TabContentPane` から注入する案を採用**（理由: QML テストにはコンテキストプロパティが無く、直読みすると `tst_MainDialogs.qml` から触れない）。**`highlighted` は宣言的バインディングでは効かないことが実測で判明** — `DialogButtonBox` が取り込んだ子 Button に `highlighted` を命令的に書くのでバインディングが壊れ、`highlighted: true` すら false を返す。`onOpened` から代入する形にした。**レビュー（リンタ + エージェント 2 体）で 1 件検出し今周で修正**: 版管理の答えがダイアログを開いたあとに届くと、本文だけが「復元できない」に変わり**アクセントとフォーカスは Continue に残る**（＝Enter が破壊的な答えを撃つ）。`onContinueLosesDataChanged` で貼り直す形にし、テストを1 件追加。リンタは今周の行への指摘 0（既存行に 3 件、うち 1 件は `.arg(a, b)` を誤検知したもの）。テスト 11 件追加（ctest 644→650 件）、全部通過・警告 0 |
| 2026-08-16 | 名前衝突（コピー・移動）2／「名前を変えて実行」を両経路に足す | M | `evolve/029` | **実機確認が残っている**（`drive` は無人判定 `skip`。GUI 確認は起動＋スクショ `.screenshots/503-evolve-029.png` のみ）: ① 同名のある場所へ Ctrl+C → Ctrl+V / Ctrl+X → Ctrl+V で**ボタンが 4 つ**（Continue / Rename / Skip / Cancel）出て、狭いウィンドウでも収まるか ② 本文に**付ける名前が実際に出る**か（コピー `x - Copy.txt` / 移動 `x (2).txt`）③ Rename でその名前どおりに着地するか（移動は**1 リクエスト**でリネーム込み、MEGA の Web で版が増えていないことも確認）④ **フォルダのみの衝突でも Continue が出て、コピーで同名フォルダが 2 つ並ぶ**か（従来は Continue を隠して黙って落としていた）⑤ D&D 経路（Ctrl+ドロップのコピー／通常ドロップの移動）でも同じ 4 択か ⑥ 同一フォルダへの貼り付けが従来どおり**無確認で `- Copy`** のままか。**サフィックスは仕様どおり経路で分ける案を採用**（コピー `- Copy`＝既存の `uniqueCopyName()`、移動 `(2)`＝新設 `uniqueMoveName()`。理由: CM §3-4。共通化せず 5 行の重複を許したのは、抽象を 1 つ足すより局所的だから）。**`renamedTo` を衝突信号に足してダイアログに名前を渡す案を採用**（理由: 生成名は宛先の taken 集合が要るので QML 側では出せない。あくまで**予告**で、回答時は宛先を読み直して選び直す）。**`startCopyBatch` の `entry.isFolder` 取りこぼしを撤去**（`evolve/028` が次へ送った宿題。CM §1-2 コピー×フォルダ＝同名 2 つ）。**レビュー（リンタ + エージェント 2 体）で 2 件検出し、どちらも今周で修正**: ① 移動の Rename が**衝突していないエントリの名前を claim しておらず**、同一バッチ内で `x.txt`→`x (2).txt` と、そのまま入る `x (2).txt` が衝突しうる ② コピーの予告と実際の計画で claim 規則が違い、**予告した名前と違う名前**になりうる（どちらもテストを 1 件ずつ追加）。リンタは変更ファイル 0 件（`tools/megatool.cpp` に既存行の指摘 5 件、今周の行ではないので不介入）。テスト 12 件追加、644 件全部通過・警告 0 |
| 2026-08-15 | 名前衝突（コピー・移動）1／移動の置き換えを撤去して「このまま移動」にする | S | `evolve/028` | **実機確認が残っている**（`drive` は `flag`＝許可あり、だが Ctrl+X → 移動 → ダイアログ → ボタンという多段のキー/クリック合成が必要で命中率が低いため呼ばず、GUI 確認は起動＋スクショ `.screenshots/502-evolve-028.png` のみ）: ① 同名ファイルのある場所へ Ctrl+X → Ctrl+V で出るボタンが **Replace ではなく Continue** になっているか ② Continue で**移動先に同名が 2 行並び**、ゴミ箱には何も入らず、バージョン履歴も増えないか ③ **同名フォルダ**の移動でも Continue が出て（従来はフォルダのみの衝突で隠れていた）2 つ並ぶか ④ 移動元に張った公開リンクが**切れなくなった**か（CM §4-2。ハンドルが変わらなくなったのがこの周の主目的の 1 つ）⑤ 本文が move のとき「"Continue" leaves both: ...」に切り替わるか ⑥ D&D 経路でも同じか ⑦ コピー側は挙動不変（同名ファイル＝版が積まれる）のままか。**「このまま実行」を表す名前は `moveIgnoringExisting` / `copyIgnoringExisting`、enum は `Proceed` を採用**（理由: `Replace` は API に無い操作を指す語で、CM §7-1 が廃止した概念そのもの。既存の `*SkippingExisting` と同じ `<動詞><動名詞>Existing` の族に揃えた）。**コピー経路は改名だけで挙動に触っていない**——衝突フォルダを「このまま実行」でも落とす取りこぼしが残っているが、CM §7-3（一貫性は経路の中だけで取る）に従い、ダイアログを常時表示にする「名前衝突 2」の行にメモとして送った。ボタン表示条件は移動側だけ常時表示（`operation === "move" \|\| conflictingFiles.length > 0`）。`canMove` は `FileOperationService::move` が内部で通しているので `moveOne` から消えても検証は落ちていない。**レビュー（リンタ + エージェント 1 体）で欠陥 0、指摘 2 件を今周で修正**（ヘッダの継続行インデント／移動側に「全部衝突して 1 件も発行されない」テストが無くなっていた＝1 件追加）。テスト 634 件全部通過・警告 0 |
| 2026-08-15 | 名前衝突（コピー・移動）1／移動が同名兄弟を作ることを実測する | S | （手作業、ループ外） | **実測は完了、確認は残っていない。** `megatool` に `mv` を足して測った（`megatool.exe mv <path> <folder>`、名前チェックを一切しないのが要点——アプリ側は全経路が衝突ダイアログへ逸れるので、素の `moveNode` を同名の上へ撃つ手段が他に無かった）。結果は**予想どおり**: ① 3 バイトの `a.txt` を 9 バイトの `a.txt` がある `dst` へ移動 → `dst` に `a.txt` が **2 行**（`9` と `3`）、版は積まれず移動元は空 ② `inside.txt` 入りの `photos` を空の同名 `photos` がある `dst2` へ移動 → `photos` が **2 行**、結合なし。どちらもエラー・警告なしで `moveNode` は素直に成功する。`SPEC_NAME_CONFLICT_COPY_MOVE.md` §1-2 に実測表として記録し、同 §6-1 の未検証項目は削除した。**この 1 件だけループの 2 時間枠に載せず手作業でやった**（実測自体は 1 分で、枠に見合わないため）。フィクスチャ `/MegaExplorerFixture/mv` は片付け済み（ゴミ箱にはある）。テスト 636 件全部通過・警告 0 |
| 2026-08-15 | 名前衝突 2／移動の置き換えを「コピー→移動元をゴミ箱」に変える | M | `evolve/027` | **実機確認が残っている**（`drive` は無人判定 `skip`。QML を触っていないので GUI 確認も行っていない）: ① 同名ファイルのある場所へ Ctrl+X → Ctrl+V →「Replace」で、**移動先の古いファイルがゴミ箱ではなくバージョン履歴に入る**か（MEGA の Web で版を見るのが確実。`evolve/024` の実機確認②が「ゴミ箱に入る」だったのを、この周が逆に作り替えている）② ゴミ箱に入るのが**移動元だけ**になっているか ③ 移動元の行が消えて移動先が 1 行のままか ④ D&D 経路でも同じか ⑤ **移動元のノードハンドルが変わる**ので、移動元に公開リンクを張っていたら切れる（SPEC §4-3 の既知の差異、仕様どおりか目視）⑥ バイト同一のファイルを置き換えたとき（SDK が no-op で `API_OK` を返す経路）でも移動元がちゃんと消えるか。**`canMove` の事前チェックは残す案を採用**（理由: 破壊的な段が②に移ったので `evolve/024` が入れた「宛先のファイルだけ失う順序」の防波堤としては不要になったが、移動元を消してよいかを答えるのはこのチェックだけで、外すと「コピーは通ったが移動元が残る＝ただの複製」に落ちる）。`DestinationSnapshot::files` は `map<name,handle>` → `set<name>` に縮小（ゴミ箱送りの相手を名前で引く必要が消えたため。ROADMAP が言う「Replace が古い名前でゴミ箱送りの相手を選ぶ」バグはこれで構造的に消滅）。**レビュー 2 段（リンタ + エージェント 2 体）で 3 件、うち 2 件を今周で修正**（`moveOne` の旧挙動を説明したままのヘッダコメント／コピー成功・ゴミ箱失敗の分岐にテストが無い＝テスト 1 件追加）。**1 件は ROADMAP「優先度: 中」末尾へ起票**（その半端な状態のトーストが「移動に失敗」としか言わない）。テスト 636 件全部通過・警告 0 |
| 2026-08-14 | 切り取り→貼り付けの衝突ダイアログを Cancel するとクリップボードが空になる | S | `evolve/026` | **実機確認が残っている**（`drive` は無人判定 `skip`。QML を触っていないので GUI 確認も行っていない）: ① Ctrl+X → 同名のある場所で Ctrl+V → Cancel で、切り取りのゴースト表示が残り**もう一度貼り付けられる**か ② Skip / Replace で答えたあとはクリップボードが空になるか ③ 宛先の読み取りに失敗したとき（読み取り専用の共有など）に切り取りが残るか ④ ダイアログを開いたまま別のファイルを切り取り直し、そのあと Skip で答えても新しいほうの切り取りが消えないか。**「印をどこに持たせるか」は状態を足さず、発行地点で突き合わせる案を採用**（`startMoveBatch` がバッチを出す直前に、クリップボードの cut とバッチの entries を**ハンドルの集合として一致するときだけ** `clear()` する。理由: 経路を通す旗を `moveNameConflict` の信号と QML の回答に往復させると、D&D 経路と共有の 3 メソッドの署名が全部変わる）。**全部スキップされてバッチが 1 件も出ない場合はクリップボードを残す**（何も動いていないため）。**レビューで再入の穴を検出し今周で修正**——クリップボードが即座に空になることが「宛先読み取り中の 2 回目の Ctrl+V」を塞ぐ役目も兼ねていたので、`mCutPasteReadInFlight` を 1 個足して同じ窓を塞いだ（ダイアログが出ている間は modal のためキーが届かない）。テスト 5 件追加、635 件全部通過・警告 0 |
| 2026-08-14 | 名前衝突 1／アップロードの上書きを SDK のバージョン機構に任せる | M | `evolve/025` | **SPEC §6-2 の未検証事項を先に実測し、「バグ修正」と確定した**（`megatool` で `/MegaExplorerFixture/vercheck` に `ver.txt`（3 バイト）を上げ、同名 9 バイトを上げ直すと**行は 1 本のまま 9 バイトに変わる**＝兄弟が増えず SDK が版を積んでいる。旧ノードは版として生き続けるので、その直後の `moveToRubbish(replaceHandle)` は「版をゴミ箱へ捨てていた」ことになる。検証用フォルダは片付け済み）。**`replaceHandle` 機構を全部削除**: `UploadJob::replaceHandle`・`UploadService::enqueue` の引数・`Batch::pendingReplaces`／`replaceFailed`・`enqueueAll` の `claimedReplaceHandles` 重複排除・`uploadReplaceFailed` トースト（QML の case ごと）。**`UploadController` から `FileOperationService` の注入も外した案を採用**（理由: 唯一の用途がそのゴミ箱送りで、残すと「まだ削除する経路がある」という誤読になる。呼び出しは `main.cpp` とテスト 2 本の計 3 箇所だけ）。**残る実機確認**: ① 同名ファイルをドロップ →「Replace」で、**新しい内容が出て古いほうがバージョン履歴に入り、ゴミ箱には入らない**か（この周の主目的。MEGA の Web で版を見るのが確実）② 置き換え直後にフォルダのスピナーが**転送完了と同時に止まる**か（従来はゴミ箱送りの完了まで回り続けていた）③ 「Uploaded, but some of the files being replaced could not be removed」のトーストがもう出ないか ④ 別フォルダ由来の同名ファイル 2 つを同時に置き換えたときの重複（SPEC §6-3、ROADMAP「名前衝突 8」で別途対応）。**レビュー 2 件とも今周で修正**（`collisionsFor()` の戻り値からハンドルが死んだので `std::set<QString>` へ縮めた／`collisionsFor()` のコメントが「replace は指していない物を消す」と、もう存在しない削除を説明していた）。テストは 2 本を作り替え・1 本を削除（630 件全部通過、警告 0） |
| 2026-08-14 | 名前衝突 2／移動（D&D）経路にダイアログを通す | M | `evolve/024` | **実機確認が残っている**（`drive` は無人判定 `skip` のため呼んでいない。GUI 確認は起動＋スクショ `.screenshots/500-evolve-024.png` のみ）: ① 同名ファイルのある場所へドラッグ&ドロップすると Replace / Skip / Cancel が出るか ② Replace で**古いほうがゴミ箱に入り**（バージョン履歴ではない——コピー経路と挙動が違う点）新しいほうが 1 行だけ残るか ③ 同名フォルダのドロップで Replace が消え Skip / Cancel の 2 択になるか ④ Ctrl+X → Ctrl+V でも同じダイアログが出るか ⑤ 本文の「The other N item(s) are moved either way.」が move 側の文面になっているか ⑥ **移動のたびに宛先の一覧読み取りが 1 回増えた**ので、大きなフォルダへのドロップで体感が落ちていないか。**`copyNameConflict` とは別に `moveNameConflict` を立て、ダイアログ 1 つで両方受ける案を採用**（理由: 1 つの信号に `operation` 判別子を足すと、コピーには意味の無い source/sourceIsRoot が常に乗る。ファイル名 `CopyConflictDialog.qml` は ROADMAP の行が指定した「共通形」をそのまま使う意図で据え置き）。**宛先読み取りに失敗したら移動ごと中止**（コピー経路の `copyEntriesTo` と同じ判断。cut-paste でもキャッシュに退避しない——古い一覧で移動を出すことが、この項目が防ごうとしている同名兄弟そのものを作る）。**レビュー 4 件のうち 2 件を今周で修正**: ① ゴミ箱送りの前に `canMove` を通すようにした（同期・ローカルで返るので、通らない移動のために宛先のファイルだけ失う順序を潰した）② `invokeOnGuiThread` へ渡す内側のラムダが `self` を捕まえておらず、タブを途中で閉じると `shared_from_this()` が `bad_weak_ptr` を投げて `std::terminate` する経路があった——**同じ形がコピー経路 3 箇所に前からあった**ので併せて修正（項目外だが 1 語ずつ、放置すると次の周が同じ罠の上に積む）。**残る 2 件は ROADMAP の「優先度: 高」末尾へ起票**（Cancel でクリップボードが消える／Replace が古い名前でゴミ箱送りの相手を選ぶ）。テスト 8 件追加、631 件全部通過 |
| 2026-08-14 | `evolve/020` が入れたダイアログ幅の回帰テストが素通りする | S | `evolve/023` | **テストのみの変更で、製品コードは 1 バイトも動いていない**（`git diff` は `tst_MainDialogs.qml` だけ）。**素通りしないことを変異テストで実測した**——`NameConflictDialog.qml` の `Label` から `width` の行を外して再ビルドすると `verify(label.width <= dialog.availableWidth)` で落ち、戻すと 622 件全部通る（修正前の版はこの状態でも通っていた）。**`evolve/021` の形をそのまま複製せず、行数の判定だけ変えた案を採用**（理由: この本文は件数の文と名前一覧の間に `"\n"` を持つので、`lineCount > 1` は折り返しが無くても成立してしまう。`label.text.split("\n").length`＝本文自身のハード改行数と比べる形にして、マジックナンバー 2 を書かずに済ませた）。ガード `label.implicitWidth > dialog.parent.width` も生きていることを同じ実行で確認（落ちたのはその先の行）。**残る確認**: 無し——実機で見るものはこの周には無い（テストの網の張り直しのみ。ダイアログ自体の実機確認は `evolve/020`・`evolve/022` の行に残っているものがそのまま有効） |
| 2026-08-14 | `NameConflictDialog` の implicitHeight にバインディングループが出ている | S | `evolve/022` | **実機で確認済み**（`drive`、フラグ有り。スクショ `.screenshots/497`）: 760px のウィンドウで `/500` を全選択してコピー → `/100` へ貼ると衝突ダイアログが出て、本文（100 件・名前 5 件＋「…」）が 4 行に折り返されフレーム内に収まり、Replace / Skip / Cancel の 3 つとも画面内。Esc で取り消して `/100` は 101 件のまま（アカウント無変更）。**バインディングループは 8 件（`NameConflictDialog`）＋ 3 件（`MissingPinDialog`）から 0 件へ**（`MegaExplorerQmlTests -o -,tap` で実測。テスト全体で残る警告は `PreviewPane` の image provider 未登録の 1 件のみで、これは従前からのテスト環境由来）。**`evolve/021` が削除確認ダイアログ 2 つで実証した形をそのまま複製する案を採用**（理由: 新しい抽象や共通基底を起こさず、`Label` の `width` が読む先を `root.availableWidth` からオーバーレイ由来の `maxWidth` へ差し替えるだけで済む。ROADMAP の行自体がこの形を直し方として指定していた）。**行が名指ししていない `CopyConflictDialog` も同時に直した**（理由: `evolve/019` が入れた同一形のコピーで、テストに載っていないぶん警告が出ていなかっただけ。放置すると次の周が同じ罠の上に積む）。**バインディングループ自体の回帰テストは足していない**——Qt の警告は QML テストから観測できず、`ignoreWarning` 系で捕まえられる形になっていないため。**残る確認**: ① `MissingPinDialog` の実機確認（ピン先フォルダをセッション中に消す必要があり `drive` では作れなかった。長いフォルダ名で本文が折り返されるか）② `NameConflictDialog`（アップロード側）の実機確認——ドロップ経由でしか開かず `drive` はプロセス外の D&D を合成できない ③ 折り返し後の本文とボタンの余白バランス（`/ui-style` 案件） |
| 2026-08-14 | 削除確認ダイアログが長いファイル名ではみ出す | S | `evolve/021` | **`ConfirmRubbishDialog` は実機で確認済み**（`drive`、フラグ有り。スクショ `.screenshots/490`）: 96 文字のファイル名を 1 件選んで Delete を押すと、ダイアログが 760px のウィンドウに収まり本文が 2 行に折り返され、はい／キャンセルとも画面内。**残る実機確認**: ① `ConfirmPermanentDeleteDialog`（ゴミ箱画面）側の同じ確認——ゴミ箱内の 544 件から目的のファイルを絞れず（**検索がゴミ箱に効かない**、別件の可能性あり）`drive` では開けなかった。構造は 1 文字違いで単体テストは通っている ② 複数選択時（件数を出す文面）で不自然な折り返しが起きないか ③ 折り返し後の本文とボタンの余白バランス（`/ui-style` 案件）。**`Label` の幅を `root.availableWidth` ではなくオーバーレイ由来の `maxWidth` から採る案を採用**（理由: `availableWidth` を読むと `implicitHeight` のバインディングループが 1 実行あたり 49 件出る。実測で 0 件になる形を選んだ）——`NameConflictDialog` の既存パターンからの意図的な逸脱で、バグ行にも直し方として書き戻した。**回帰テストは「フレームの幅」ではなく「本文が折り返されたか」で見る形にした**——`evolve/020` 型の `dialog.width <= parent.width` は Popup 自身の切り詰めのせいで修正が無くても通ることを実測したため（その素通りするテスト自体をバグ行に起票）。**副産物**: 触った 2 ダイアログのバインディングループは 6 件・2 件から 0 件になった |
| 2026-08-14 | アップロードの名前衝突ダイアログが狭いウィンドウからはみ出す | S | `evolve/020` | **実機確認が残っている**（このダイアログはドロップからしか開かず、`drive` はプロセス外のドラッグ&ドロップを合成できないため GUI 確認は起動のみ）: ① 760px 程度までウィンドウを狭め、長い名前のファイルを同名のある場所へドロップして Replace / Skip / Cancel の 3 つとも画面内に収まるか ② 本文が折り返されて読めるか ③ 衝突 6 件以上（本文が最長になるケース）でも収まるか ④ 右のコピー側 `CopyConflictDialog` と幅の出方が揃って見えるか。**`evolve/019` が `CopyConflictDialog` に入れた 1 行をそのまま複製する案を採用**（理由: 共通の基底ダイアログを起こすと 2 箇所のためだけに抽象が増え、次の周がその上に積む。ROADMAP の行自体が「同じ 1 行でよい」と指定していた）。**回帰テストを `tst_MainDialogs.qml` に 1 件追加**——このダイアログはドロップ経由でしか開かずスクショで捕まえられないので、`dialog.width <= dialog.parent.width` を突き合わせるのが唯一の網。**副産物として 2 件をバグ行に起票**: ① `ConfirmRubbishDialog` / `ConfirmPermanentDeleteDialog` が同じ罠を踏んでいる（`Label` に `width` も `wrapMode` も無い）② `NameConflictDialog` の `implicitHeight` にバインディングループが 8 件出ている——**今周の修正より前から出ている**ことを、修正を外して測り直して確認済み |
| 2026-08-14 | 名前衝突 1／コピー経路を新仕様に合わせる | M | `evolve/019` | **実機で確認済み**（`drive`、フラグ有り。スクショ `.screenshots/478`・`479`・`485`・`486`）: ① 同名フォルダのある場所へフォルダを貼ると衝突ダイアログが出て **Replace が消え Skip / Cancel の 2 択**になる ② 同名ファイルでは Replace / Skip / Cancel の 3 択 ③ **同一フォルダへの貼り付けは無確認で `- Copy`**（トースト「Copied 1 item(s)」）④ 760px のウィンドウでもダイアログがはみ出さない。テスト用フィクスチャはゴミ箱へ片付け済み。**衝突判定は「同じ種別で同名」を採用**（理由: ファイルとフォルダは MEGA 上で共存でき互いを上書きも併合もできないので、種別違いは衝突ではなく `uniqueCopyName` で `- Copy` に逃がすほうが安全。`evolve/017` の「フォルダ名は衝突に混ぜない」修正の意図をそのまま残せる）。**`copyRenamingExisting()` は削除**（`Keep both` が消えて QML から誰も呼ばなくなったため。`CopyConflict::Rename` は非衝突コピーの既定エンジンとして残っている）。**`cachedChildNames(bool)` を `cachedChildren()` に置き換え**——同一フォルダ判定に宛先のハンドル集合が要るので、名前だけでは足りない。**レビューで QML の 2 件を検出し今周で修正**: ① `DialogButtonBox` は非表示のボタンも `contentModel` の数に入れて幅を等分するので、`visible` だけではボタン 1 個分の穴が残る（`alignment: Qt.AlignRight` + `implicitWidth` を 0 に潰す形にした。`width` を上書きするとボタンが細片になって描画される）② Popup が content の implicit 幅を採るためダイアログが本文の最長行まで広がり、狭いウィンドウではボタンが画面外へ出る（`width` に上限を入れた。**同じ問題を持つアップロード側 `NameConflictDialog.qml` はバグ行として ROADMAP に起票**）。**残る確認**: ① ファイルとフォルダが混ざって衝突したときの本文（「"Replace" overwrites the files only.」の行）——テストアカウントで混在を作れず単体テストのみ ② 6 件以上の衝突で「…」が出るか ③ フッターが右寄せ（自然幅）に変わったので、アップロード側の等分レイアウトとの差が気にならないか（`/ui-style` 案件）④ SPEC §4-2 のフォルダ・マージ喪失が実使用で許容できるか |
| 2026-08-14 | 名前衝突の解決（`SPEC_NAME_CONFLICT_RESOLUTION.md`）を S/M へ分割 | L→分割 | `evolve/018` | **実装はしていない**（L の分割のみ。コード変更 0）。**経路ごとに割る案を採用**（理由: SPEC の表は「コピー／移動／アップロードで Replace の実現手段が違う」ことが軸で、選択肢の設計は 1 回で済む。ダイアログ形状ごとに割ると 3 経路のどれもが半端に動く周ができる）。5 行に分け、**1（コピー）を先頭に固定**——共通形はここで決まるので、2〜5 は 1 の後でしか着手できない。件数・サイズ表示（SPEC §3-4）と §6-3 の重複 Replace 対策は**本体から切り離して S 2 件**にした（どちらも独立に効き、無くても各経路は成立する）。**人間に確認してほしい**: ① この 5 分割の粒度と順序でよいか（特に「4 件数・サイズ表示」を後回しにしてよいか）② **`evolve/017` が「衝突とみなすのはファイルだけ」で入れたばかりの判断を、項目 1 が反転させる**——SPEC はフォルダも衝突扱いにする側なので、実装後はフォルダの `- Copy` 自動改名が別フォルダへの貼り付けでは起きなくなる ③ SPEC §4-2 のフォルダ・マージ喪失（再アップロードで中身が足せなくなる）を受け入れる前提のまま進めてよいか |
| 2026-08-13 | コピー時の上書き／スキップ確認ダイアログ | M | `evolve/017` | **実機確認が残っている**: ① 同名ファイルのある場所へ Ctrl+V したときに 4 択のダイアログが出るか（重複名の一覧が読めるか、5 件超で「…」になるか）② 「Replace all」で MEGA 側が**新しいバージョン**として載るか（旧版が版履歴に残り、一覧には 1 行だけ）③ 「Skip」で衝突分だけ落ち、残りは普通にコピーされるか ④ Ctrl+ドラッグのドロップでも同じダイアログが出るか ⑤ ダイアログ表示中に別のコピーを始めると 2 件目がキューに積まれて順に聞かれるか ⑥ ボタンが 4 つあるのでフッターが横に溢れていないか（見た目は `/ui-style` の担当）。**衝突とみなすのはファイルだけにする案を採用**（理由: MEGA では同名フォルダが共存し上書きの対象が無いため。フォルダは従来どおり黙って `- Copy` へ改名）。**「上書き」は旧ノードをゴミ箱へ送らず、同名でコピーして SDK の版管理に載せる案を採用**（理由: アップロード側〈Phase 14b〉と違いコピーは 1 リクエストで完結し、二段階の非原子性を持ち込まずに済む）。ダイアログはタブごとに 1 個（`TabContentPane.qml`、`NewFolderDialog` と同じ置き方）。**レビューで「衝突判定に宛先の *フォルダ* 名まで混ざっており、同名フォルダのある場所へファイルをコピーすると偽の衝突が出る（Skip を選ぶとそのコピーが黙って消える）」を検出し今周で修正**——「改名時に避ける名前の集合」と「上書きしうるファイル名の集合」を分けた
| 2026-08-13 | 転送のキャンセル導線 | M | `evolve/016` | **実機確認が残っている**（`drive` は無人判定で呼んでいないため、GUI 確認は起動のみ）: ① 大きめのファイルをダウンロード中にステータスバーの ✕ を押して転送が実際に止まるか ② 「Cancelled N download(s)」トーストの N が押した時点のキュー長と合うか ③ 複数ファイルをキューに積んだ状態でキャンセルすると失敗トーストが N 枚出ないか ④ アップロード側で同じ 3 点 ⑤ キャンセル後に次の転送が正常に開始できるか（トークンを毎回作り直しているかの確認）。**「方向ごとに全部キャンセル」を採用**（理由: 直列キューなので同時に走るのは方向あたり 1 件だけ、ジョブ単位のキャンセルを掛ける UI がそもそも無い。並列化の行にその旨を追記済み）。**キャンセルは失敗ではなく `Cancelled` 状態として扱う**——`kEIncomplete` を SDK 側の合図として使い、トーストは押した瞬間に 1 枚だけ出す。**`MegaCancelToken` の所有をリスナから `MegaSdkClient` へ移した**（リスナは SDK スレッドで自己 delete するので、外から掴めない）。**レビューで見つけた競合を同サイクルで修正**——ジョブが `mActive` になってから `download()` 内でトークンが作られるまでの隙にキャンセルが来ると 1 つ前のトークンを叩いて空振りするため、`mCancelGeneration` を置いて呼び出し後に再送する（アップロード側は間に同期の `checkUpload` が挟まるぶん隙が広い）。単体テスト 4 件追加 |
| 2026-08-13 | ダイアログが開いている間に 2 回目のドロップをすると 1 回目が黙って消える（バグ） | S | `evolve/015` | **実機確認が残っている**（`drive` はプロセス外のドラッグ&ドロップを合成できないため、GUI 確認は起動のみ）: ① ダイアログ表示中に別フォルダへドロップし、1 枚目に答えたあと 2 枚目が続けて出るか ② 2 枚目の宛先・件数が 2 回目のドロップのものになっているか ③ 1 枚目を Cancel しても 2 枚目が出るか。**3 択のうち「キューに積む」を採用**（理由: 残り 2 案——2 回目を断る / ドロップを受けない——はどちらもユーザーの操作を捨てることになり、捨てた事実を伝える導線が別途要る。積めば何も失われない）。**両ダイアログに同じ 10 行を複製**（共通コンポーネントは作らない: payload の形が違い、2 箇所しかない）。**`onClosed` を唯一の起点にした**——`NameConflictDialog` は 3 ボタンとも `close()` を呼ぶので、ここだけ見れば全経路をカバーできる。QML テスト 3 ケース追加（順送り・Cancel でも次が出る・置換/スキップ側） |
| 2026-08-13 | アップロードが複数ファイルのときの確認ダイアログ | M | `evolve/014` | **実機確認が残っている**（`drive` はプロセス外のドラッグ&ドロップを合成できないため、GUI 確認は起動のみ）: ① 2 個以上をドロップすると「Upload N file(s)?」が出て、OK で上がり Cancel で何も上がらないか ② フォルダをドロップしたとき N が中身の数になっているか ③ 同名ファイルがあるときに確認 → 置換/スキップの順で 2 枚出るか ④ 101 件以上では確認が出ず上限トーストだけが出るか。**1 ファイルのときは訊かない案を採用**（理由: フォルダへ 1 個ドラッグするたびにクリックが増える）。**上限チェックが先で、超過時は確認を出さない**（上限は質問ではなく拒否）。**`uploadConfirmed` は QML から戻ってきた件数を信じず再カウント**（置換/スキップの handler と同じ防御）。**レビュー指摘 1 件は ROADMAP のバグ行に起票**——ダイアログ表示中に 2 回目のドロップをすると 1 回目が黙って消える（`NameConflictDialog` も同じ形の既存問題だが、今回の確認ダイアログで露出が上がった）。QML 側は `tst_MainDialogs.qml` に 3 ケース追加（シグナル配線・OK・Cancel）——閉じたダイアログはスクショで見えないので、ここが唯一の網 |
| 2026-08-13 | 検索結果からフォルダへ移動しても検索欄にクエリが残る（バグ） | S | `evolve/013` | **実機で確認済み**（`drive`、フラグ有り。スクショ `.screenshots/468`〜`470`）: ① 検索 → 結果のフォルダをダブルクリックで検索欄が空になり、空フォルダの案内文も「This folder is empty」側に戻る ② 検索したまま更新ボタンを押すとクエリも結果も保持される（案内文は「No items match your search」）。**「移動したらクエリを落とす」案を採用**（理由: Explorer と同じで、もう一方の「移動先で検索し直す」はフォルダを開いた結果が中身にならず驚く）。**落とすのはナビゲーション入口 6 つだけ**（`loadRoot` / `openFolder` / `openFavourites` / `openRubbish` / `goBack` / `navigateToKind`）——更新系は `refreshVisibleListing` が検索を保つ設計なので触らない。**`mListingFromSearch` は残した**（`evolve/012` で入れたメニュー用フラグ。今は検索欄と答えが一致するが、「行が何か」を表すのはこちらなので正）。**残る確認**: ① タブを切り替えたときに検索欄のテキストがそのタブの状態に追従しない（**既存の別問題**、今回は変えていない） |
| 2026-08-13 | 「フォルダに移動」コンテキストメニュー | M | `evolve/012` | **実機で確認済み**（`drive`、フラグ有り。スクショ `.screenshots/461`〜`466`）: ① 検索結果の右クリックに「Go to folder」が出る ② 実行すると親フォルダ（`100`）へ移動し、パンくず・タブ名が更新され、対象行が選択される ③ 移動先では同じ行がメニューから消える ④ お気に入り一覧からも出て、深い階層（`Test Folder > my second folder > foo`）へ正しく移動し選択される。**残る確認**: ① 一覧が長くスクロールが要る場合に対象行まで実際にスクロールするか（`revealRowRequested` 経由。今回は画面内だった）② グリッド表示での同じ挙動。**リゾルバに 3 本目の軸を足すのではなく `bool crossFolderOnly` を足す案を採用**（理由: target/arity と違って中間値が無い）。**「どの一覧が cross-folder か」は検索欄ではなくモデルの中身から決める**——`search()` は仕様上クエリを保持したまま移動できるので、検索欄を見ると移動先の普通のフォルダでも行が出続ける（レビューで検出し、今周で修正）。**検索欄のクエリが移動後も残る件は既存の挙動**なので触らず、ROADMAP の「バグ修正」に起票した |
| 2026-08-13 | ファイルアイコンの種類（画像・テキスト・PDF・zip 等） | M | `evolve/011` | **実機で一部確認済み**（スクショ `.screenshots/459`）: `.jpg` に写真、`.txt` に書類、未登録の拡張子が汎用ページに落ちることを確認。**残る確認**: ① 動画・音声・PDF・zip・コード・表計算の 6 種を実際に置いて見分けが付くか（テストアカウントに検体が無く未確認）② **書類（U+E8A5）と汎用ページ（U+E7C3）が 16px では似て見える**——`.txt` と未登録ファイルの区別が付きにくいので、グリフの選び直しは `/ui-style` 案件 ③ グリッド表示（48px）での見え方。**新しい singleton `qml/FileTypeIcons.qml` を置く案を採用**（理由: `Theme.qml` は純粋なトークン置き場で、拡張子の照合という手続きが混ざる。1 種類 = 1 ブロックにフォント名・コードポイント・拡張子が並ぶ形にした）。**フォント名は種類ごとに指定可能で、既定値が `Theme.font.iconFamily`**——Segoe Fluent Icons に無い絵が要る種類だけ別フォントを名乗れる、というのが要望の「ペア」の意図。**コードポイントは両フォントの cmap 実測に加えて 16px で描画して確認**（`docs/DESIGN_IMPROVEMENT.md` §11 の「cmap は必要条件でしかない」に従った）。**折り返し的に `.jfif` / `.jpe` / `.tgz` を追加**（スクショで `.jfif` が汎用ページに落ちているのが見えたため） |
| 2026-08-12 | フォルダのアップロード（再帰） | M | `evolve/010` | **実機確認が残っている**: ① フォルダをドロップすると中身ごと上がるか（`megatool` で階層を確認）② 中身 101 件以上のフォルダで上限トーストが出て 1 件も上がらないか ③ ファイル 1 件＋中身 100 件のフォルダ、で上限に当たるか ④ アップロード中のフッターにフォルダ名が出て、進捗バーが動くか（フォルダは初期サイズ 0 で入れているため）⑤ 「Folders can't be uploaded」ダイアログがもう出ないか。**`FolderDropDialog.qml` は削除**（フォルダをスキップするためだけの確認で、存在理由が無くなった。歯止めは件数上限が担う）。**フォルダは名前衝突の対象から外す案を採用**（理由: MEGA はファイルとフォルダが同名で共存できるので、同名ファイルを「置換」するとユーザーが指していない物を消す）。**フォルダの `expectedTotalBytes` は 0**（ディレクトリの `size()` は中身と無関係。0 は既に「未知」の意味で、SDK の進捗が来たら上書きされる）。**件数は再帰的に数え、上限を超えた時点で打ち切る**ので、20 万件のツリーでも 101 件と同じコスト。**1 ドロップ＝1 ジョブ**なので、フォルダの中身は個別ジョブにならず、完了トーストの件数もドロップした項目数のまま。**レビューで 2 件検出し今周で修正**: ① Windows のショートカット（`.lnk`）はフォルダを指していると `isDir()` が true を返すのに SDK には `.lnk` 自体が渡るので、フォルダ扱いのまま名前衝突の確認を素通りしていた（判定を `isFolderUpload()` 1 箇所に集約）② 同名のファイルとフォルダを同時にドロップして「スキップ」を選ぶと、質問の対象ですらないフォルダまで巻き添えで消えていた |
| 2026-08-12 | アップロード件数の上限（100 件程度） | S | `evolve/009` | **実機確認が残っている**: ① 101 件以上をまとめてドロップすると「Too many files — upload at most 100 at a time」のトーストが出て 1 件も上がらないか ② 100 件ちょうどは通るか ③ フォルダを混ぜた 101 件のドロップで、フォルダのスキップ確認ダイアログが**出ずに**上限のトーストが出るか。**上限超過は全体を拒否する案を採用**（理由: 先頭 100 件だけ黙って上げると、何が上がって何が落ちたか画面から分からない）。**上限値は C++ 側の `UploadController::kMaxFilesPerUpload` の 1 か所だけに置き、トーストの文言へは `maxFilesPerUpload` プロパティ経由で注入**（QML に数字を書くと 2 か所が食い違いうる）。**チェックは `dropUrls` と `uploadFiles` の 2 か所**（`uploadFiles` が唯一の関門だが、そこだけだとフォルダのスキップ確認に答えた後で拒否されるため）。**フォルダは再帰アップロード未実装なので、今は「ドロップに含まれる素のファイル数」に対する上限**——再帰アップロードを入れる周で、展開後の件数にも同じ上限をかけ直すこと |
| 2026-08-12 | ゴミ箱ビュー — 完全削除と「ゴミ箱を空にする」 | M | `evolve/008` | **実機確認が残っている**（`drive` フラグが無く未実施）: ① ゴミ箱内のアイテムを右クリック →「Delete permanently」→ 確認ダイアログ → 実際に消えるか（`megatool` で件数確認）② ゴミ箱の空白部分の右クリックに「Empty Rubbish bin」が出るか ③ 左ペインの「Rubbish bin」行の右クリックに同じものが出るか ④ **ゴミ箱の中のフォルダを開いた状態で空にすると、そのフォルダは消えるのでゴミ箱の先頭へ戻す実装にした**——実際に戻るか ⑤ 2 つのアイコンが「ゴミ箱へ移動」と同じゴミ箱グリフのままなので見分けが付くか（`/ui-style` 案件）。**「ゴミ箱を空にする」は `cleanRubbishBin` 一発を採用**（理由: 子を列挙して 1 件ずつ `remove` すると途中終了で半端に空いたゴミ箱が残る。サーバー側で完結する）。**確認ダイアログは件数を出さない**（数えるには一覧要求が要り、その答えは Yes を押す頃には古い）。**他タブへのファンアウトは付けていない**（`evolve/007` の復元と同じ判断: ゴミ箱を同時に 2 タブで開く頻度が低い）。**ゴミ箱内のサブフォルダでも「Empty Rubbish bin」は出る**（リゾルバは「ゴミ箱の先頭にいるか」を見られないため。確認文が何を消すか明示している）。副次的に **ROADMAP「ゴミ箱で右クリックすると空のメニュー」も解消**——真因は `MenuActions::toViewKind` が `Rubbish` を変換せず空リストを返していたことで、そこを直した。**Delete キーは完全削除に割り当てていない**（要望に無く、キー 1 つで破壊的操作が走るため） |
| 2026-08-12 | ゴミ箱ビュー — 復元アクション | M | `evolve/007` | **実機で確認済み**（`drive`）: ゴミ箱で右クリック → 「Restore」が出る（それまで空だったメニューが埋まった）→ 実行で項目がゴミ箱から消え（529→528）、トースト「Restored 1 item(s)」、`megatool` で元の場所（Cloud Drive 直下）に戻っていることを確認。テスト用に戻した項目はゴミ箱へ戻して現状復帰済み。**元の親が消えている場合は Cloud Drive のルートへ戻す案を採用**（理由: 拒否するとゴミ箱から出す手段が他に無く詰む。MEGA 公式クライアントも同じ挙動）。**ただし黙って別の場所に置かない**——その場合はトーストの文言を `restoreToRoot` に切り替えて「元のフォルダが無いので Cloud Drive に戻した」と明示する。レビューで `getRestoreTarget` に `mShuttingDown` ガード漏れ（このファイルの不変条件）を検出し今周で修正。**残る確認**: ① 元のフォルダが消えているケースの文言（テストアカウントに再現データが無く、単体テストでのみ検証）② 複数選択での復元 ③ 復元後に他のタブが自動更新されない（現在のタブのみ再読込。復元は頻度が低いと判断してファンアウトを付けていない） |
| 2026-08-12 | ゴミ箱ビュー — 一覧表示と導線 | M | `evolve/006` | **実機で確認済み**（`drive`）: 左ペインの「Rubbish bin」行から一覧が開き（529 件）、タブ名・パンくず・行のハイライトが揃うこと、ゴミ箱内のフォルダに潜れてパンくずが「Rubbish bin > 〜」になることを確認。ゴミ箱内の右クリックでメニューが空（アクション 0 件）になること、ゴミ箱内のフォルダから「上へ」でゴミ箱の先頭に戻ることも確認。**残る確認**: ① Ctrl+C / Delete / F2 もメニュー同様に効かないか（`canPerform` 経由なので理屈上は同じ） ② パンくずの「Rubbish bin」と次のセグメントの間に区切り記号が出ていない（`navigable` でない先頭セグメントの見た目）③ ゴミ箱内は全セグメントが非クリックになるため、パンくずで途中階層に戻れない。**ゴミ箱内のフォルダを開いても kind を Rubbish のまま持ち越す案を採用**（理由: ゴミ箱の中身は普通のノードなので、kind だけが「破壊的操作を許すか」の判定材料。CloudDrive に戻すとゴミ箱の中で「ゴミ箱へ移動」が出てしまう）。復元・完全削除は別項目のまま。**レビューで 2 件（どちらも「isRoot＝Cloud Drive のルート」という古い前提の取りこぼし）を検出し、今周で修正**: ゴミ箱内フォルダから「上へ」が Cloud Drive のルートへ飛ぶ／パンくずの先頭以外に kind が付かず入れ子のゴミ箱フォルダが CloudDrive 扱いになる（＝「ゴミ箱へ移動」が出てしまう） |
| 2026-08-12 | ドラッグ中のホバーでツリーの行を展開する（スプリングロード） | M | `evolve/005` | **実機確認が残っている**: ① ファイルをドラッグしてツリーの折りたたまれた行の上で 0.7 秒静止すると展開するか ② 素通りしただけの行が展開しないか ③ 展開後にそのまま子フォルダへドロップできるか ④ 700ms が体感で長すぎ/短すぎないか。**`drive` では検証不能**——`drag` が終点で即座にボタンを離すので 0.7 秒の静止を作れず、静止を作る唯一の方法（終点をツリー行にする）は実際のドロップ＝ファイル移動になってしまうため。展開/折りたたみ自体が壊れていないことと、`hasChildren` が正しく入っている（子の無い `500` にシェブロンが出ない）ことは `drive` で確認済み。**メモにあった「クイックアクセスのオートスクロール」は既に実装済み**（`QuickAccessSection.qml`）だったので、この周はツリーのスプリングロードのみ。**タイマーはツリーに 1 個だけ置く案を採用**（理由: 行ごとに持つと通過した全行にタイマーが残る）。**レビューで「行番号を 700ms 持ち越すと別のフォルダを開きうる」と判明し、デリゲート＋ハンドルを保持して発火時に照合する形へ作り直した**（行番号は上の行が展開しただけでずれ、しかもその際ドラッグイベントは再配信されない） |
| 2026-08-12 | バンド選択の水平方向オートスクロールと、Esc でのキャンセル | S | `evolve/004` | **実機確認が残っている**: ① 横スクロールが要る幅までウィンドウを狭め、バンドを左右の端へ持っていくとスクロールするか ② ドラッグ中に Esc を押すとバンドが消え、**開始前の選択に戻る**か（ボタンを離す前に押すこと）③ Esc 後にボタンを押したまま動かしても選択が壊れないか。**いずれも `drive` では検証できない**——`drag` が終点で即座にボタンを離す作りで、「端で保持する」「ドラッグ中に別のキーを送る」ができないため。バンド操作自体が壊れていないことは `drive` で確認済み（4 行選択）。**軸ごとに `DragAutoScroller` を 2 個置く案を採用**（理由: 共有コンポーネントを二軸対応に改造すると 5 箇所の呼び出し全部に影響が出る。スクロール不要な軸では既存のクランプが no-op になる） |
| 2026-08-12 | 背景の右クリックメニューに「更新」「すべて選択」「貼り付け」を追加 | S | （実装済みのため行を削除） | **コード確認のみで完了扱い**にした。`MenuActionResolver.cpp` に `NewFolder` / `Paste` / `SelectAll` / `Refresh` の 4 つが `MenuSite::FolderBackground` で登録済みで、同日のスクショ（`.screenshots/434`）にも 4 行とも出ている。ROADMAP の行が古かっただけ |
| 2026-08-12 | 通常の空フォルダにも空状態の表示を出す | S | `evolve/003` | **実機確認 済**（2026-08-12、`drive` で①②③とも確認。②は空→中身ありの向きで検証、残留なし）: ① 空フォルダを開くと「This folder is empty」と「Drop files here, or right-click and choose "New folder".」が出るか ② 中身のあるフォルダへ移動したときに一瞬でも出ないか（読み込みガードを入れない判断をしたため、ここが唯一の賭け）③ 文言の上に重ねた状態で背景の右クリックとドロップが効くか。**「読み込み済みか」のガードは入れない案を採用**（理由: 一覧取得はメモリ内読み出しで再描画前に終わる、と既存の busy プロパティが宣言しているため。②が外れていたらガードを足す）— 設計判断を含むので、人間がフェーズにまとめるか判断してよい |
| 2026-08-12 | フォルダ名の入力中に同名が既にあるかその場でチェックする | S | `evolve/002` | **実機確認が残っている**: ① 既存フォルダ名を打つと灰色の注意文がその場で出るか、消すと消えるか ② 同名の**ファイル**では出ないか ③ OK を押して kEExist が返ると同じ文が赤に変わるか。**警告だけで OK は無効化しない案を採用**（理由: 判定は手元のキャッシュなので、古い「取られている」で本来作れる名前を塞ぐほうが害が大きい。サーバーの kEExist が最終判断のまま）— 設計判断を含むので、人間がフェーズにまとめるか判断してよい |
| 2026-08-11 | フォルダ作成後にそのフォルダを選択・スクロール表示する | S | `evolve/001` | **実機確認が残っている**: ① 作成したフォルダが実際に選択され、一覧が長いときにスクロールして見えるか（リスト／グリッド両方、非表示側から切り替えたときも）② 検索中に作ると reveal は行わない仕様なので、検索結果の表示が壊れないか。レビューで「作成した名前を状態として保持すると別フォルダの一覧に誤爆する」と判明し、リクエストに載せる形へ作り直した — 設計判断を含むので、人間がフェーズにまとめるか STUDY にするか判断してよい |
