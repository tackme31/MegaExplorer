# 受信箱 — 要望・機能追加・不具合

**思いついたことをそのまま書き足す場所。** `/evolve` ループが毎サイクルの最初にここを読み、
`docs/ROADMAP.md` へ反映してから、この行を消す。**ROADMAP.md を直接編集する必要はない。**

書き方は**自由記述の箇条書き**、1 件 1 項目。サイズ・優先度・置き場所は決めなくてよい（それを
決めなくて済むのがこのファイルの存在理由）。決めたいときだけ下のラベルを頭に付ける。

置き場は **`master`**。書いたら**コミットまで済ませること**——未コミットの変更が残っていると
次のサイクルは冒頭のワーキングツリー検査で中止する（安全側に倒れるだけだが、その周は無駄になる）。
サイクル実行中にコミットすると、その周の `git merge --ff-only` が拒否される。通常マージすれば
済むだけで、対処は `CLAUDE.md` の「Loop engineering」にある。

## 付けられるラベル（全部任意）

| ラベル | 意味 |
|---|---|
| `[バグ]` | 不具合。ROADMAP の「バグ修正（最優先）」へ入る。**再現手順を書くこと** |
| `[最優先]` | そのセクションの先頭に置く。**これが無い限り、ループは末尾にしか追加しない** |
| `[高]` `[中]` `[低]` | 優先度セクションの指定。無ければ「中」の末尾 |
| `[削除]` | ROADMAP から消す。どの項目かわかるように書く |
| `[編集]` | 既存項目の書き換え・メモの追記。どの項目かわかるように書く |

ラベルなしの新規要望は「優先度: 中」の末尾に入る。**早く着手してほしいものは必ずラベルを付ける**
——ループは自分で置いた行を既存の行より上には置かない（優先順位を決めるのは人間だけ、という
線引きのため）。

## ループ側の約束

- **サイズは付けるが、分割はしない。** 画面 1 つ分以上ありそうなものは `L` として登録し、
  ROADMAP の既存ルール（L はそのまま着手せず、1 周かけて S/M へ割る）に任せる。
- **意味が取れなかったら推測しない。** その行は消さずに残し、末尾に
  `← 解釈できませんでした（理由）` を付けて報告に書く。印の付いた行は次の周から読み飛ばすので、
  書き直すときはその印ごと消すこと。
- **取り込んだ行は消える。** 消えていれば ROADMAP に入ったということ。何がどう入ったかは
  サイクルの報告に出る。

---

## 未処理

<!-- ここから下に書く。ループが取り込んだら消える。 -->

- [バグ] 何もないフォルダに貼り付けても重複確認ダイアログが出る
  現状の挙動:
    複数選択してコピー → 空のフォルダに貼り付けると、貼り付け先には何も無いのに重複確認
    ダイアログが出る。再現は `tips` の copytest 以外を全部コピーし、copytest で貼り付け。
    重複扱いされるのは bafkreib…webp / HNQbymGbAAATNdK.jfif / HOTGSWDboAA2n9G.jfif の 3 件。
  原因:
    その 3 件は `tips` の中に同名で 2 つずつ実在する（MEGA は同一フォルダ内の同名を許す）。
    `FileMutationController::collidingEntries` の `broughtTwice` が、貼り付け先ではなく
    **貼り付ける側の集合の中**の同名 2 つ目を衝突として数えるため、貼り付け先が空でも出る。
    仕様どおりの判定ではある（SPEC_NAME_CONFLICT_COPY_MOVE 5-3）。同 6-1 が
    「2 件目を自動リネームするか説明文を足すか」を未決で残していて、今回はそこを踏んだもの。
  期待する動作:
    集合内に同名（名前＋種別が同じ）が複数あるコピー／移動は、**そもそも禁止**する。
    貼り付け（およびドロップ）の時点で「同じ名前のファイル/フォルダを複数コピー／移動する
    ことはできません」と警告し、操作全体を実行しない。どれが重複しているかは既存の
    衝突名リスト（ConflictNameListDialog）と同じ形式で見せる——次に何をすればいいかは
    それで判断できる。レアケースなので禁止で構わない、と判断した。
    宛先の既存ノードとの衝突は今までどおり重複確認ダイアログで訊く。この禁止が入れば
    `broughtTwice` 由来の衝突はダイアログに到達しなくなるので、6-1 も一緒に閉じる。
