# 受信箱 — 要望・機能追加・不具合

**思いついたことをそのまま書き足す場所。** `/evolve` ループが毎サイクルの最初にここを読み、
`docs/ROADMAP.md` へ反映してから、この行を消す。**ROADMAP.md を直接編集する必要はない。**

書き方は**自由記述の箇条書き**、1 件 1 項目。サイズ・優先度・置き場所は決めなくてよい（それを
決めなくて済むのがこのファイルの存在理由）。決めたいときだけ下のラベルを頭に付ける。

置き場は **`develop`**（開発の幹。`master` はリリース済みの版専用）。書いたら**コミットまで
済ませること**——未コミットの変更が残っていると次のサイクルは冒頭のワーキングツリー検査で中止する（安全側に倒れるだけだが、その周は無駄になる）。
サイクル実行中にコミットすると、その周の `git merge --ff-only` が拒否される。通常マージすれば
済むだけで、対処は `CLAUDE.md` の「Loop engineering」にある。

## 付けられるラベル（全部任意）

| ラベル | 意味 |
|---|---|
| `[バグ]` | ROADMAP の種類欄が「不具合」になる。**再現手順を書くこと**。優先度は別で、`[高]` `[中]` `[低]` が無ければ「中」 |
| `[最優先]` | そのセクションの先頭に置く。**これが無い限り、ループは末尾にしか追加しない** |
| `[高]` `[中]` `[低]` | 優先度セクションの指定。無ければ「中」の末尾 |
| `[削除]` | ROADMAP から消す。どの項目かわかるように書く |
| `[編集]` | 既存項目の書き換え・メモの追記。どの項目かわかるように書く |

ラベルなしの新規要望は「優先度: 中」の末尾に入る。**早く着手してほしいものは必ずラベルを付ける**
——ループは自分で置いた行を既存の行より上には置かない（優先順位を決めるのは人間だけ、という
線引きのため）。

## ループ側の約束

- **サイズは付けるが、分割はしない。** 画面 1 つ分以上ありそうなものは `L` として登録し、
  ROADMAP の既存ルール（L はそのまま着手せず、1 周かけて S/M へ割る）に任せる。
- **意味が取れなかったら推測しない。** その行は消さずに残し、末尾に
  `← 解釈できませんでした（理由）` を付けて報告に書く。印の付いた行は次の周から読み飛ばすので、
  書き直すときはその印ごと消すこと。
- **取り込んだ行は消える。** 消えていれば ROADMAP に入ったということ。何がどう入ったかは
  サイクルの報告に出る。

---

## 未処理

<!-- ここから下に書く。ループが取り込んだら消える。 -->

- `[最優先]` `[高]` **アプリ内ビューアを別ウィンドウにする。** いまの `evolve/097` の実装は
  ウィンドウに 1 つ重ねるオーバーレイ（`qml/components/ImageViewer.qml`）だが、これを独立した
  トップレベル `Window` にしたい。**優先度: 高 の先頭、「動画をアプリ内で再生する」の行より上に
  入れてほしい** —— 動画・PDF より先にやれば器が 1 つで済み、動画と PDF は最初からウィンドウの
  中に実装できる。逆に 3 形式そろってから変換すると、オーバーレイ固有の回避（フォーカス奪取・
  ヘッダを覆えない前提・タブ切り替えで閉じる・`DropArea`）を 3 つ分作ってから 3 つとも捨てる
  ことになる。
  調査済み: `docs/investigations/STUDY_VIEWER_SEPARATE_WINDOW.md`（実現可能性・案 B/C1/C2 の
  比較・未確認事項）。要点だけ言うと、**テーマは障害にならない**（`Theme.qml` は
  `pragma Singleton` でエンジン単位、`viewerController` も `rootContext` 経由で届く。過去に
  「別ウィンドウはテーマが壊れる」と言われていたのは `Menu` が `popupType: Popup.Window` を
  持つ場合の話で、トップレベル `Window` には当てはまらない）。**難所はキャプションバー 1 つ**
  ——本体は QWindowKit でフレームレス化してあり `CaptionBar.qml` はタブ帯を内包していて
  再利用できない。着手する周はまず STUDY §5-1 の実測（`Window` を 1 枚出して「OS ダーク ×
  `MEGAEXPLORER_COLOR_SCHEME=light`」を撮る）をやってから案を選ぶこと。

- `[編集]` **上の別ウィンドウ化にあたって、「前後のファイルへ移動」（← → キー）は当面スコープ外
  にする。** いまの画像ビューアには入っているが、別ウィンドウ版が引き継ぐ必要はない。これを
  落とすと表示に要るのは**名前と URL だけ**になり、ウィンドウがタブの `FileListModel` を握る
  必要がなくなる（タブが閉じても、フォルダを移動されても、ウィンドウが自立する）。
  使ってみて不便だったらいずれ追加するかもしれないので、**消すのではなく「やらない」という
  判断**として記録しておいてほしい。戻すときの実装方針は STUDY §4-2-1 にある（モデルを握り
  続けるのではなく、開いた時点で見られる行の (handle, name) 列をスナップショットする）。