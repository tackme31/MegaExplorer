# ROADMAP

**このリポジトリで次に何を実装するかを決める唯一のバックログ。** `/evolve` ループが毎サイクル
最初に読み、ここから 1 件選んで実装する。

**要望・機能追加・不具合は `docs/REQUESTS.md`（受信箱）に自由記述で書く。** ループが毎周それを
読んでこの表へ反映するので、**このファイルを人間が直接編集する必要はない**。書き込むのがループ
だけであれば、サイクルが完了行を消すのと人間の編集が同じ行を取り合うこともない。

## 運用ルール

1. **上から順に着手する。** セクション順（バグ修正 → 高 → 中 → 低）、セクション内は表の上から。
   飛ばすのは「サイズ **L**」「`blocked`」「前提が未達」のときだけで、その理由を報告に書く。
2. **完了したら行をこの表から消し、`docs/roadmap-done.md` に 1 行足す。** 両方に残さない。
3. **`blocked` は「やらないと決めた」か「今は着手できない」。** 必ずメモ欄に理由を書く。
   `blocked` の行はループが勝手に `todo` へ戻さない — 戻すのは人間だけ。
4. **サイズ L はそのまま着手禁止。** その周は「L を S/M へ分割して表を書き換えるだけ」で終わらせ、
   実装は次の周に回す。分割の前に、メモ欄にリンクした `docs/investigations/STUDY_*.md` を読む。
5. **バグ・要望の入口は `docs/REQUESTS.md`。** GitHub issue は使わない（双方向の同期コストに
   見合わないため）。受信箱は人間が書きループが消すだけの一方向なので、その同期は要らない。
   長い診断が要るものだけ `docs/investigations/BUG_*.md` を起こし、ここからリンクする。
6. **このリポジトリで実装しないもの**は「見送り (blocked)」節にある。ループはそこへ着手しないだけ
   でなく、**関連する機能を新たに提案することもしない**。

## 凡例

| サイズ | 目安 | 意味 |
|---|---|---|
| **S** | 1 時間相当 | 1 ファイル〜数ファイルの局所的な変更。設計判断がほぼ要らない |
| **M** | 半日相当 | 複数レイヤに跨るが、既存の型と画面の枠内に収まる |
| **L** | 要分割 | 画面 1 つ分以上、または新しい抽象が要る。**そのまま着手しない** |

---

## バグ修正（最優先）

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| — | （現在なし） | — | 入口は `docs/REQUESTS.md` に `[バグ]` + 再現手順 |

---

## 優先度: 高

先頭の 6 件は旧 `FEATURE_IDEAS.md` の「細かい改善」。どれも局所的で、既存の画面の欠けを埋める。

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| todo | フォルダ作成後にそのフォルダを選択・スクロール表示する | S | Phase 19 で新規フォルダ作成は入ったが、作成後の選択までは未対応 |
| todo | フォルダ名の入力中に同名が既にあるかその場でチェックする | S | 現状は確定時にサーバーのエラーを見るだけ（Phase 19 ログ「重複名チェックはサーバーのものが唯一」） |
| todo | 通常の空フォルダにも空状態の表示を出す | S | 今はお気に入り一覧だけが空状態を持つ（Phase 24b） |
| todo | 背景の右クリックメニューに「更新」「すべて選択」「貼り付け」を追加 | S | Phase 19 のメニュー解決器へ足す。空白部分の右クリックが2回発火する既知の癖に注意（Phase 19 ログ） |
| todo | お気に入りのハートアイコン自体のクリックでトグルできるようにする | S | Phase 24a のハートバッジは表示専用。バンド選択・行選択と競合しないヒットテストが要る |
| todo | バンド選択の水平方向オートスクロールと、Esc でのキャンセル | S | Phase 21。垂直方向のオートスクロールは実装済み |
| todo | ドラッグ中のホバーでツリーの行を展開する（スプリングロード） | M | クイックアクセスのオートスクロールも同時に。Phase 14a / 22a の drop 処理へ |
| todo | ゴミ箱ビュー — 一覧表示と導線 | M | 特殊画面の枠組みは Phase 24b で実装済み。`MegaApi::getRubbishNode()` 配下を listing する。→ `docs/investigations/STUDY_SPECIAL_VIEWS_FRAMEWORK.md`、1画面分の仕様の書き方は `SPEC_FAVOURITES_VIEW.md` が実例 |
| todo | ゴミ箱ビュー — 復元アクション | M | `MegaNode::getRestoreHandle()` が復元先ハンドルを返す。元の親が消えている場合の扱いを決めること。`IMegaClient::moveNode` を再利用 |
| todo | ゴミ箱ビュー — 完全削除と「ゴミ箱を空にする」 | M | `MegaApi::cleanRubbishBin()` が全消し。個別の完全削除は `IMegaClient` に `remove` 相当が無いので追加が要る（現状 `moveToRubbish` のみ） |
| todo | フォルダのアップロード（再帰） | M | 今はドロップ時に確認ダイアログを出してスキップしている。`startUpload` はディレクトリを受け取れるので SDK 層の追加は不要（Phase 14b ログ） |
| todo | ファイルアイコンの種類（画像・テキスト・PDF・zip 等） | M | ホワイトリスト式。Segoe Fluent Icons で足りなければフォント追加を検討 |
| todo | 「フォルダに移動」コンテキストメニュー | M | 検索結果・お気に入り一覧から、そのアイテムのある親フォルダへ遷移。スクロール＋選択状態まで再現できると良い |
| todo | 検索結果・お気に入り一覧に「場所」列を出す | M | 同名ファイルの区別。上の「フォルダに移動」と同じ「このノードはどこにあるのか」問題の一部 |

---

## 優先度: 中

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| todo | 転送のキャンセル導線 | M | `MegaCancelToken` は生成・保持まで済んでいて、キャンセルを呼ぶ経路だけが無い |
| todo | 転送の並列化とファイルごとの進捗表示 | M | 上のキャンセルとセットで考える。現状は 1 件ずつ直列 |
| todo | コピー時の上書き／スキップ確認ダイアログ | M | 選択肢は「全て上書き」「名前を変えてコピー」（既存動作）「対象をスキップ」「キャンセル」。個別選択はやらない。代わりに重複ファイル名の一覧を見せる。Phase 14b の「Replace は MEGA にネイティブでない」を読むこと |
| todo | 詳細検索 — スコープ切替（開いているフォルダ配下 / アカウント全体） | M | `MegaApi::search(MegaSearchFilter*, ...)` が再帰、`getChildren(filter, ...)` が非再帰。現状の検索がどちらを使っているか確認してから |
| todo | 詳細検索 — フィルタのポップアップ（種別・カテゴリ・日時・お気に入り） | M | `MegaSearchFilter` に `byNodeType` / `byCategory` / `byCreationTime` / `byFavourite` が揃っている。検索欄の右にボタンを置く |
| todo | 最近追加された項目のビュー | M | 特殊画面の枠組みを再利用。使う API の選定は `docs/investigations/STUDY_RECENTLY_UPDATED_FILES_SDK_API.md`（3系統から用途で選ぶ、と結論済み） |
| todo | リンクの共有 | M | `MegaApi::exportNode` で発行、`disableExport` で停止。停止の導線も同時に作る |
| todo | 設定画面 | M | ハンバーガーメニューから開くダイアログ。テーマと言語の選択 |
| todo | フォルダを指定した貼り付け・移動 | M | フォルダ行・ツリー・クイックアクセスへの「貼り付け」と、コンテキストメニューの「移動先を選択」。今はドラッグ&ドロップでしかできない |
| todo | ラベル（色）の設定と表示 | M | MEGA のノードはラベルを持てる。🏷️ 相当のアイコンを色分けして表示する |
| todo | テキストプレビューの対象拡張子を広げる | M | JSON やプログラミング言語のファイル。50KB 未満をダウンロードし、バイナリなら「プレビューできません」。→ `docs/investigations/STUDY_PREVIEW_PANE.md`（Phase 15） |
| todo | お気に入りでの並び替え | M | `ORDER_FAV_ASC/DESC` はソートではなく**グループ化**なので、既存の「列 → ソートキー」の対応には乗らない |
| todo | ツリー／クイックアクセスのフォルダ行メニューからお気に入りを設定 | S | Phase 24a のアクションを左ペインのメニューにも生やすだけ |
| todo | タブを複製 | S | タブの右クリックメニューへ。Phase 22b のタブ操作まわり |
| todo | 最大化した状態を保存し、次回起動時も最大化で開く | S | 今はサイズだけ保存している |
| todo | タイトルバーにアプリアイコンを表示する | S | Phase 17a/17b の自前キャプション行へ |

---

## 優先度: 低

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| todo | 多言語対応（日本語） | L | 現在英語のみ。全 QML の文字列を `qsTr` に通す作業量が本体。着手前に S/M へ分割すること |
| todo | アルバム | L | MEGA のアルバムの表示・作成・削除・出し入れ。**UI をどうするかが未決**なので、分割の前に仕様を決める必要がある。枠組みは `STUDY_SPECIAL_VIEWS_FRAMEWORK.md` |
| todo | リアルタイム反映 | L | 他デバイスの変更を SDK のプッシュ通知で開いている一覧へ。Phase 6 のフォルダオープン時バックグラウンド更新に足す形。**スコープ宣言（「同期クライアントではない」）との線引きを先に決める** |
| todo | アプリ外へのドラッグ&ドロップ | L | アプリ内のアイテムを Windows Explorer へドラッグしてダウンロード。Win32 の `IDataObject` 実装が要る |
| todo | OS クリップボードとの連携 | L | Explorer でコピーしたファイルをアプリ内に貼るとアップロードになる。現状の Ctrl+C/X/V はアプリ内で MEGA のノードを動かすだけ（Phase 23） |
| todo | ローカルフォルダとの紐づけ | L | 「ローカルファイルの場所を開く」導線。MEGA 上のフォルダとローカルの対応をユーザーに設定させる必要がある（MEGAsync の設定を読むのは多分無理） |
| todo | Mica / Acrylic の背景を適用する | L | `FluentWinUI3` のパレットと合わせるのが手間で後回しにしている。見た目の作業なので**ループのスコープ外**（`docs/DESIGN_IMPROVEMENT.md` 側の話） |
| todo | プラグイン対応 | L | 一番重い。コンテキストメニュー項目の追加・独自プレビュー・設定画面の追加を外部から。トースト／ダイアログ／ファイル更新の API 公開も要る。動機は WD Tagger による自動タグ付けで、ファイラーのドメインから外れるためデフォルト同梱にしたくない、というもの |

---

## 見送り (blocked)

**ここはループが着手しない。提案もしない。**

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| blocked | 「もとに戻す」アクション（undo） | L | **スコープ外。** MEGA にネイティブな undo が無く、全操作に手書きの逆操作（rename↔rename / rubbish→戻す / copy→delete / create→delete）と、全変更経路への記録フック、履歴破棄のポリシーが要る。Phase 23（コピー／カット／貼り付け）と同じ要望から出たが、そこで落とした。→ `docs/PROGRESS.md` の「Phases 20a–23 — the detail pass」前文。`CLAUDE.md` のスコープ宣言もこれに従う |
| blocked | ローカルとの完全な双方向同期 | L | **スコープ外。** このアプリは同期クライアントではない（フォルダを開いたときの単発バックグラウンド更新だけ） |
| blocked | タブをウィンドウの外へ切り離す | L | **スコープ外。** → `docs/investigations/STUDY_TITLEBAR_TABS.md` |
