# ROADMAP

**このリポジトリで次に何を実装するかを決める唯一のバックログ。** `/evolve` ループが毎サイクル
最初に読み、ここから 1 件選んで実装する。

**要望・機能追加・不具合は `docs/REQUESTS.md`（受信箱）に自由記述で書く。** ループが毎周それを
読んでこの表へ反映するので、**このファイルを人間が直接編集する必要はない**。書き込むのがループ
だけであれば、サイクルが完了行を消すのと人間の編集が同じ行を取り合うこともない。

## 運用ルール

1. **上から順に着手する。** セクション順（高 → 中 → 低）、セクション内は表の上から。
   飛ばすのは「サイズ **L**」「`blocked`」「前提が未達」のときだけで、その理由を報告に書く。
   **種類は着手順に影響しない** —— 不具合が機能追加より先に来るのは、優先度が上のときだけ。
2. **書くのは「満たすべき機能」であって、実装方法ではない。** 項目名とメモは
   **何ができるようになれば完了か**を述べる場所で、ファイル名・関数名・クラス設計・手順といった
   実装の具体は書かない —— それを決めるのは着手する周が現物のコードを読んでからで、先に書くと
   古びるうえ判断も縛る。例外は**在り処の手掛かり**だけ（`evolve/NNN` が残した穴、`STUDY_*.md`
   へのリンク、既存仕様のどこが変わるか）。受信箱から取り込むときも同じ。
3. **完了したら行をこの表から消し、`docs/roadmap-done.md` に 1 行足す。** 両方に残さない。
4. **`blocked` は「やらないと決めた」か「今は着手できない」。** 必ずメモ欄に理由を書く。
   `blocked` の行はループが勝手に `todo` へ戻さない — 戻すのは人間だけ。
5. **サイズ L はそのまま着手禁止。** その周は「L を S/M へ分割して表を書き換えるだけ」で終わらせ、
   実装は次の周に回す。分割の前に、メモ欄にリンクした `docs/investigations/STUDY_*.md` を読む。
6. **種類は「意図と実装が一致しているか」で決める**——深刻度でも、直す手間でもない。
   - **不具合** ＝ **既に実装した機能が、実装した周が意図した振る舞いをしていない。** 誰もそう
     動くつもりが無かった挙動のこと——文字化け、表示が古いまま残る、押しても何も起きない、
     落ちる、状態が別のタブへ漏れる、等。
   - **機能追加** ＝ **今の挙動は意図どおりだが、それでは足りない。** まだ書いていない振る舞いを
     新しく足す話。
   - 迷ったら **「その挙動を、それを入れた周の完了条件として書けたか」** を問う。書けない
     （＝誰も望んでいない）なら不具合。書けるが物足りないだけなら機能追加。
7. **種類と優先度は独立に決める。** 不具合だから高、ではない。優先度は**重要性**で決める:
   - **高** ＝ 落ちる・データが失われる・その機能が常に使えない・誤った結果を正しいものとして
     見せる。
   - **中** ＝ 回避策があるか、起きる条件が限られる。
   - **低** ＝ 見た目の粗、稀なケース、実害が小さいもの。
   深刻度の見立てや手当ての順序で補足が要るならメモ欄に書く。サイズ L の不具合も 5. のとおり
   まず分割する。
8. **サイクル中に気づいた意図しない挙動は、その周で直さないなら種類「不具合」で起票する。**
   実装中・検証中・レビュー中・実機確認中のどこで気づいたかを問わない。6. の基準で不具合なら
   **機能追加として登録しない**。優先度は 7. の目安で振り分ける。メモ欄には**再現手順**と
   **気づいた周（`evolve/NNN`）**を書く。原因が未特定でも起票する（未特定だと書く）。
9. **バグ・要望の入口は `docs/REQUESTS.md`。** GitHub issue は使わない（双方向の同期コストに
   見合わないため）。受信箱は人間が書きループが消すだけの一方向なので、その同期は要らない。
   長い診断が要るものだけ `docs/investigations/BUG_*.md` を起こし、ここからリンクする。
10. **このリポジトリで実装しないもの**は「見送り (blocked)」節にある。ループはそこへ着手しないだけ
    でなく、**関連する機能を新たに提案することもしない**。
11. **項目名とメモに個人情報と実データの名前を書かない。** アカウント名・メールアドレス・
    ローカルのパス、および実データやテスト用データのファイル名・フォルダ名は、再現手順であっても
    書かない —— **ファイル名そのものが個人情報を含みうる**ため。「PDF を 1 つ」「深くネストした
    フォルダ」のように条件だけを述べる。受信箱に書かれていても、取り込むときに言い換えて落とす。
    `docs/roadmap-done.md` も同じ。

## 凡例

| サイズ | 目安 | 意味 |
|---|---|---|
| **S** | 1 時間相当 | 1 ファイル〜数ファイルの局所的な変更。設計判断がほぼ要らない |
| **M** | 半日相当 | 複数レイヤに跨るが、既存の型と画面の枠内に収まる |
| **L** | 要分割 | 画面 1 つ分以上、または新しい抽象が要る。**そのまま着手しない** |

| 種類 | 意味 |
|---|---|
| **機能追加** | 今の挙動は意図どおりだが、それでは足りない |
| **不具合** | 実装した周が意図した振る舞いをしていない |

---

## 優先度: 高

| 状態 | 種類 | 項目 | サイズ | メモ |
|---|---|---|---|---|
| todo | 機能追加 | アプリ内ビューア — 動画をアプリ内で再生する | M | 土台（`evolve/096`）は入った。ダブルクリックの動線と、ウィンドウに 1 つ重ねるオーバーレイの器は `evolve/097` が画像で敷いた（`PreviewKind::Video` も既にある）ので、そこに乗せるか別の器にするかは着手する周が決めてよい。**完了条件**: 動画をダブルクリックすると再生でき、一時停止と任意位置へのシークが効く。**実機確認が要る**（自動検証では音と絵が出ているか分からない）。配布物に同梱するものが増えるので`scripts/package.ps1` の確認まで含めて完了とする。`STUDY_INAPP_VIEWER.md` §3-2、および §6 に未確認事項が 2 件（細い回線で再生が勝手に止まる可能性、実行時に掴む DLL がどちらか）ある |
| todo | 機能追加 | アプリ内ビューア — PDF をアプリ内で開く | M | 土台（`evolve/096`）は入った。ダブルクリックの動線と器は `evolve/097` が画像で敷いた（`PreviewKind::Pdf` も既にある）。**完了条件**: PDF をダブルクリックすると中身が表示され、ページを送れる。**Qt PDF は開発環境にインストール済み**（`evolve/096` で `C:/Qt/6.11.1/msvc2022_64/lib/cmake` に `Qt6Pdf` / `Qt6PdfQuick` を確認。STUDY 本文が「未インストール」と書いていた前提は解消済み）。手段は `STUDY_INAPP_VIEWER.md` §3-3。同 §4 の重量級ブラウザエンジンを入れる案は**採らない** |

---

## 優先度: 中

| 状態 | 種類 | 項目 | サイズ | メモ |
|---|---|---|---|---|
| todo | 不具合 | 失敗したときのトーストの文面が実態に合わず、原因が分からない | M | 受信箱より（2 点まとめて）。(a) 転送量・容量の超過（`EOVERQUOTA` = -17）が `src/mega/MegaErrorCodes.h` に無いため `Unknown` に落ち、SDK の英語文字列がそのまま出るか何も出ない。無料アカウントは転送量制限に日常的に当たるので、試した人が最初に踏みやすい。(b) `BulkOperationRunner` が `errorCode` を捨てて件数だけ通知するので、容量超過も権限不足も通信失敗も「Failed to move 3 item(s)」という同じ文面になる（ダウンロード／アップロードも同様）。**完了条件**: 代表的なエラーコードが文面に反映され、原因の区別が付く。(b) 側は設計どおりの挙動なので、そこは足りない情報を足す話 |
| todo | 不具合 | アップロードの同名衝突ダイアログに既定ボタンとフォーカスが無い | S | 受信箱より。`qml/components/NameConflictDialog.qml` の `DialogButtonBox` は Replace / Skip / Cancel の 3 ボタンに `highlighted` もフォーカス指定も無い。左端が Replace（＝上書き。版管理 OFF なら旧ファイルは復元不能）なので、Enter や Tab の挙動がプラットフォーム任せになる。対になる `qml/components/CopyConflictDialog.qml` は `markDefaultAnswer()` で既定とフォーカスを明示し、データを失う場合だけ既定を安全側へ倒している。**完了条件**: 版管理 OFF のときの既定が Skip になり、開いた時点でフォーカスが定まる（2 ダイアログで実装を共有してよい） |

---

## 優先度: 低

| 状態 | 種類 | 項目 | サイズ | メモ |
|---|---|---|---|---|
| todo | 機能追加 | 多言語対応（日本語） | L | 現在英語のみ。全 QML の文字列を `qsTr` に通す作業量が本体。着手前に S/M へ分割すること。**設定画面の「言語」セレクタもここに含める**——`evolve/041` の設定画面はテーマだけで出した（`.ts` も `QTranslator` も無い段階では選択肢が 1 つしか無いため）。置き場は `SettingsDialog.qml` の GridLayout に 1 行足すだけ |
| todo | 機能追加 | アルバム | L | MEGA のアルバムの表示・作成・削除・出し入れ。**UI をどうするかが未決**なので、分割の前に仕様を決める必要がある。枠組みは `STUDY_SPECIAL_VIEWS_FRAMEWORK.md` |
| todo | 機能追加 | リアルタイム反映 | L | 他デバイスの変更を SDK のプッシュ通知で開いている一覧へ。Phase 6 のフォルダオープン時バックグラウンド更新に足す形。**スコープ宣言（「同期クライアントではない」）との線引きを先に決める** |
| todo | 機能追加 | アプリ外へのドラッグ&ドロップ | L | アプリ内のアイテムを Windows Explorer へドラッグしてダウンロード。Win32 の `IDataObject` 実装が要る |
| todo | 機能追加 | 詳細検索 — 検索範囲を選ぶドロップダウン | M | 受信箱の [編集] より（旧「スコープ切替（開いているフォルダ配下 / アカウント全体）」）。検索欄に範囲を選ぶドロップダウンを置き、「このフォルダと配下」「このフォルダのみ」「全体」の 3 つから選べるようにする（表示文字列は着手する周が決めてよい）。**`evolve/084` が詳細検索のポップアップに入れた「This folder only」チェックボックスは、これに置き換わる**。`MegaApi::search(MegaSearchFilter*, ...)` が再帰、`getChildren(filter, ...)` が非再帰。現状の検索がどちらを使っているか確認してから。**優先度「中」から降格**（Cloud Drive を開いて検索すれば足りるため） |
| todo | 機能追加 | ラベル（色）の設定 | M | 受信箱の [編集] より（旧「ラベル（色）の設定と表示」を設定／表示に分割し、「優先度: 中」から降格）。コンテキストメニューに「ラベル」を追加し、ネストしたメニューへ 未設定 / 赤 / オレンジ / 黄色 / 緑 / 青 / 紫 / グレー を並べる。各項目の左に色付きの丸アイコン（Fluent Icons `ea3b` CircleFill）。MEGA のノードはラベルを持てるので、設定の口を **SDK 層**（`IMegaClient`）に足すところから |
| todo | 機能追加 | ラベル（色）の表示 | M | 受信箱の [編集] より（同上の分割）。**リスト表示**: 行の一番左端（アイコンの左）に 3px の縦棒を色で描く（`\| [名前] [更新日時] [サイズ]` の先頭の縦棒を色付けする形）。ラベル無しなら塗らないので、その行だけ左マージンが 3px 増えて見えるが**それでよい**。今の 8px ほどの左マージンはそのまま。**グリッド表示**: ファイル名の左に色付きの丸アイコン（`ea3b` CircleFill）。アイコン用の場所を先に空けておく必要はなく、有無で名前の位置がずれてよい。`FileEntry` がラベルを運ぶ必要があるので「ラベル（色）の設定」と同じ土台に乗る |
| todo | 不具合 | アップロードで「スキップ」が、このアップロード自身が作るフォルダの中では効かない | M | `evolve/069` が残した穴。1 回のドロップに同名のローカルフォルダが 2 つあり、宛先にその名前が無いとき、中の重複は数えられるようになったが**「スキップ」は実行できない**——生き残るファイルの親になるフォルダがまだ MEGA に無く、ハンドルで親を名指しする計画に載らないため、エラーのトーストで止まる（2 つ目のコピーが丸ごと重複なら、そのコピーを落として正常に通る）。直すには計画が「N 番目の項目が作るフォルダの中へ」を表現できる必要があり、転送キュー側に依存関係の仕組みが要る。「置き換え」は今までどおり通るので、実害はスキップが選べないことだけ |
| todo | 機能追加 | サムネイルのキャッシュ化 | M | 受信箱より。一度取得したサムネイルをキャッシュし、2 回目以降は再取得しない。再取得するのは ①「リアルタイム反映」で更新が入ったとき ② ツールバーのリフレッシュを明示的に押したとき（そのフォルダのぶんだけ捨てる。再帰的には捨てない）の 2 つだけ。`ThumbnailService` は既にプロセス内に handle→パスのキャッシュを持っているので、足りないのは再起動を跨ぐぶん。①の「リアルタイム反映」は本表で未着手の L なので、それが入るまで再取得の口は②だけになる |
| todo | 機能追加 | 設定画面からキャッシュを削除する | S | 受信箱より。設定画面にキャッシュを削除するボタンを足す。現段階で消す対象はサムネイルキャッシュのみ。**「サムネイルのキャッシュ化」が前提**——それが入るまで消すものが無い |

---

## 見送り (blocked)

**ここはループが着手しない。提案もしない。**

| 状態 | 種類 | 項目 | サイズ | メモ |
|---|---|---|---|---|
| blocked | 機能追加 | 汎用の「もとに戻す」（undo） | L | **スコープ外。** `evolve/056` で入った**「移動・削除トーストの『もとに戻す』」はこれとは別物**で、履歴を持たずトーストが指す 1 操作だけを戻すもの。混同しないこと。 MEGA にネイティブな undo が無く、全操作に手書きの逆操作（rename↔rename / rubbish→戻す / copy→delete / create→delete）と、全変更経路への記録フック、履歴破棄のポリシーが要る。Phase 23（コピー／カット／貼り付け）と同じ要望から出たが、そこで落とした。→ `docs/PROGRESS.md` の「Phases 20a–23 — the detail pass」前文。`CLAUDE.md` のスコープ宣言もこれに従う |
| blocked | 機能追加 | ローカルとの完全な双方向同期 | L | **スコープ外。** このアプリは同期クライアントではない（フォルダを開いたときの単発バックグラウンド更新だけ） |
| blocked | 機能追加 | タブをウィンドウの外へ切り離す | L | **スコープ外。** → `docs/investigations/STUDY_TITLEBAR_TABS.md` |
| blocked | 機能追加 | プラグイン対応 | L | **保留（スコープ外ではない）。** 現状未知の部分が多く大幅な機能改修が予想されるため、十分な仕様が固まるまで着手しない。中身: コンテキストメニュー項目の追加・独自プレビュー・設定画面の追加を外部から。トースト／ダイアログ／ファイル更新の API 公開も要る。動機は WD Tagger による自動タグ付けで、ファイラーのドメインから外れるためデフォルト同梱にしたくない、というもの |
| blocked | 機能追加 | 書庫ファイルの中身一覧プレビュー — 7z / rar | L | 受信箱の [編集] より（旧「zip / 7z / rar」から分離）。**保留（スコープ外ではない）。** まず zip 版を出し、7z / rar はそれぞれ独立した追加として後から判断する。7z は目録の位置こそ明快だがヘッダ自体が LZMA で圧縮されていて展開器が要り、rar は目録が末尾に無く逐次走査＋独自の名前符号化が要る → `docs/investigations/STUDY_ARCHIVE_PREVIEW.md`（§2.2 / §2.3 / §6） |
| blocked | 機能追加 | お気に入りでの並び替え | M | **保留（スコープ外ではない）。** 並び替え／フィルタを出すいい導線が思いつかず、適当に作ると邪魔になるので、UI が決まるまで止める。技術的な地雷は別にあって、`ORDER_FAV_ASC/DESC` はソートではなく**グループ化**なので、既存の「列 → ソートキー」の対応には乗らない |
| blocked | 機能追加 | 目録が巨大な zip を選ぶと、その目録を丸ごとメモリへ読み込む | S | 受信箱の [編集] より「優先度: 低」から見送りへ移動。**対応しないと決めたが、懸念事項として残す。** `evolve/080` のレビューで出た。zip プレビューの 2 回目の範囲読みは目録の大きさをそのまま要求する。`ZipListing` の検査で「目録は EOCD の手前で終わる」ことは保証されているのでファイルサイズは超えないが、**エントリ数が数十万ある実在の書庫では目録そのものが数百 MiB になりうる**。閾値を入れる話は `STUDY_ARCHIVE_PREVIEW.md` §5 |
| blocked | 機能追加 | Mica / Acrylic の背景を適用する | L | `FluentWinUI3` のパレットと合わせるのが手間で後回しにしている。見た目の作業なので**ループのスコープ外**（`docs/DESIGN_IMPROVEMENT.md` 側の話）。受信箱の [編集] より「優先度: 低」から見送りへ移動 |
