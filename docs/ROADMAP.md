# ROADMAP

**このリポジトリで次に何を実装するかを決める唯一のバックログ。** `/evolve` ループが毎サイクル
最初に読み、ここから 1 件選んで実装する。

**要望・機能追加・不具合は `docs/REQUESTS.md`（受信箱）に自由記述で書く。** ループが毎周それを
読んでこの表へ反映するので、**このファイルを人間が直接編集する必要はない**。書き込むのがループ
だけであれば、サイクルが完了行を消すのと人間の編集が同じ行を取り合うこともない。

## 運用ルール

1. **上から順に着手する。** セクション順（バグ修正 → 高 → 中 → 低）、セクション内は表の上から。
   飛ばすのは「サイズ **L**」「`blocked`」「前提が未達」のときだけで、その理由を報告に書く。
2. **書くのは「満たすべき機能」であって、実装方法ではない。** 項目名とメモは
   **何ができるようになれば完了か**を述べる場所で、ファイル名・関数名・クラス設計・手順といった
   実装の具体は書かない —— それを決めるのは着手する周が現物のコードを読んでからで、先に書くと
   古びるうえ判断も縛る。例外は**在り処の手掛かり**だけ（`evolve/NNN` が残した穴、`STUDY_*.md`
   へのリンク、既存仕様のどこが変わるか）。受信箱から取り込むときも同じ。
3. **完了したら行をこの表から消し、`docs/roadmap-done.md` に 1 行足す。** 両方に残さない。
4. **`blocked` は「やらないと決めた」か「今は着手できない」。** 必ずメモ欄に理由を書く。
   `blocked` の行はループが勝手に `todo` へ戻さない — 戻すのは人間だけ。
5. **サイズ L はそのまま着手禁止。** その周は「L を S/M へ分割して表を書き換えるだけ」で終わらせ、
   実装は次の周に回す。分割の前に、メモ欄にリンクした `docs/investigations/STUDY_*.md` を読む。
6. **バグ・要望の入口は `docs/REQUESTS.md`。** GitHub issue は使わない（双方向の同期コストに
   見合わないため）。受信箱は人間が書きループが消すだけの一方向なので、その同期は要らない。
   長い診断が要るものだけ `docs/investigations/BUG_*.md` を起こし、ここからリンクする。
7. **このリポジトリで実装しないもの**は「見送り (blocked)」節にある。ループはそこへ着手しないだけ
   でなく、**関連する機能を新たに提案することもしない**。

## 凡例

| サイズ | 目安 | 意味 |
|---|---|---|
| **S** | 1 時間相当 | 1 ファイル〜数ファイルの局所的な変更。設計判断がほぼ要らない |
| **M** | 半日相当 | 複数レイヤに跨るが、既存の型と画面の枠内に収まる |
| **L** | 要分割 | 画面 1 つ分以上、または新しい抽象が要る。**そのまま着手しない** |

---

## バグ修正（最優先）

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|

（現在なし）

---

## 優先度: 高

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|

（現在なし）

---

## 優先度: 中

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| todo | 転送フライアウトの最小化表示を出し、ステータスバーの転送 UI を撤去する | S | 転送が始まったらウィンドウ**右下**に「進捗バー + 件数（9/41 のような）」のバーが自動で出る（進捗はダウンロードとアップロードを合算、母数は現在キューにある件数）。全部終わったら数秒後に自動で消える。閉じても転送は止まらない。More メニューからも開ける。**トーストと同じ隅を使うので、その隅の座標を持つ主体は 1 つに保ち、トースト側が上へ譲る** —— 3 つの Popup が同じ隅を奪い合った S10 の失敗が `ToastStack.qml` 冒頭に書いてある。ステータスバーの転送 UI（ファイル名・進捗・キャンセル）はこの項目で外す |
| todo | 転送フライアウトを展開してファイルごとの進捗を見る | M | 最小化 ⇔ 展開の切り替えと、ファイルごとの行・進捗・状態。**一括キャンセル**はここに置く。セッション中に終わった転送も見える。転送が 1 件も無いときの見た目と、ウィンドウが小さいときの高さの上限（超えたらスクロール）まで |
| todo | 転送を 1 件だけキャンセルできるようにする | M | 一覧の行から、その 1 件だけ止められる。今は全キャンセルしか無い。SDK は「今飛んでいる転送を止める」口しか持たないので、待機中の 1 件は始めずに済ませる形になる。キャンセルされた 1 件も、他と同じく完了通知が 1 回だけ出ること（件数を数えている呼び出し側が特別扱いせずに済む） |
| todo | 転送の並列化 | M | ダウンロード・アップロードを並列に走らせる。**「転送を 1 件だけキャンセルできるようにする」の後に実装する**（直列のうちは「今飛んでいる転送」を指すだけでキャンセルが成り立つが、並列だと SDK 側に転送を名指しする口が要る） |
| todo | 上書き確認ダイアログのファイル名一覧の出し方を変える | M | 現状は全件をカンマ区切りで並べるため、対象が多いと確認しきれず視認性も悪い。単一なら従来どおりファイル名を表示、複数なら `N file(s)` をクリッカブルにして、クリックで**ファイルアイコン＋ファイル名のリスト**を出す二次ダイアログを開く。名前衝突まわりでダイアログ本文が全面的に変わるのを待っていたが、**その前提は済んだので着手してよい**（`evolve/025`〜`036`） |
| todo | `ILocalFileSystem::listDirectory()` に失敗の伝え方を持たせる | S | `evolve/033` のレビューで出た穴。読めないディレクトリと空のディレクトリが同じ「空の一覧」で返るので、アップロードの「スキップ」が衝突枝を歩き直すとき（`UploadScanService::addToPlan()`）に、生き残るはずのファイルが**無言で落ちる**——走査側の同じ失敗は「訊かない」で済むのに、こちらはトーストもログも出ない。`std::optional<std::vector<LocalEntry>>` か `Result<...>` に変え、`addToPlan()` を失敗を返せる形にする。発生頻度は低いが、落ちたことが誰にも分からないのが問題 |
| todo | お気に入り一覧と「最近」一覧でも詳細検索フィルタを効かせる | S | `evolve/038` が残した穴で、`evolve/039` の「最近」画面も同じ形になった（`listRecent` もフィルタを受け取らないので、そちらもボタンを隠してある）。詳細検索のフィルタは `IMegaClient::search` にだけ通っていて、お気に入り画面の検索欄が使う `listFavourites` は受け取らない。今はボタン自体を隠して「効かないものを出さない」で凌いでいるが、種別・カテゴリ・日時の 3 つはお気に入りでも意味がある（お気に入りフラグだけが冗長）。`listFavourites` と `listRecent` に `const SearchFilter&` を足し、`MegaSdkClient` 側の `applySearchFilter()` を呼ぶだけ——ただしモックの引数が 1 つ増えるので `FolderNavigation{Service,Controller}Test` / `TabsControllerTest` の `EXPECT_CALL` 約 20 箇所の機械的な追随が要る |
| todo | 同一アップロード内の同名**フォルダ**同士の衝突を数える | S | `evolve/035` が残した穴。ファイル側は済んだが、1 回のドロップに同名のローカルフォルダが 2 つあり（別の場所から掴んだ `photos` と `photos`）、**宛先に `photos` が無い**とき、走査はどちらにも降りないので中のファイルの衝突が 1 件も出ない。実際には SDK が 2 つ目を 1 つ目に再帰マージし、同名ファイルに版が積まれる。降りるには「まだ MEGA に存在しないフォルダ」をローカル同士で突き合わせる必要があり、`scanLevel` の「MEGA のハンドルを親にして降りる」形に乗らない（ハンドルがまだ無い）——そこが設計判断なので別項目にした。宛先に `photos` が**ある**場合は両方降りるので既に正しい（`PlanTakesApartEveryCopyOfAMergingFolderName`） |
| todo | 一覧で「リンクを共有済み」が分かるようにする | M | `evolve/040` が残した穴。リンクの共有は入ったが、`FileEntry` が `MegaNode::isExported()` を運んでいないので、行を見ても共有中か分からず、右クリックの「Remove link」もリンクの無いノードに出たままになる（押しても実害は無い——`disableExport` はその場合そのまま成功を返す）。`FileEntry` にフラグを足す→`FileListModel` のロールと `entryMap`→行のマーカー（お気に入りのハートと同じ形）→`FileContextMenu` の ctx で「Remove link」を灰色に、まで一直線だが、お気に入りの `applyFavouriteChange` に相当する**その場書き換えと他タブへのファンアウト**も要る（さもないと共有直後の行が古いまま）。**マーカーの位置は受信箱で確定** —— リスト表示は Size 列の右隣に見出し無し・幅変更不可の列を 1 つ足してリンクアイコン、グリッド表示は各アイテムの右上にリンクアイコン（お気に入りアイコンと同じ要領）。アイコンの見た目そのものは `/ui-style` 側の話 |
| todo | タブを切り替えたとき検索フィルタのポップアップが前のタブの状態を映したままになる | S | `evolve/045` のレビューで出た穴で、**この周より前からある**（それまでは 4 つのコントロール自体がウィンドウ全体で 1 つの状態だったので、同じようにタブをまたいで残っていた）。フィルタはタブごとにC++ 側（`FolderNavigationController`）が持っているのに、ポップアップ側の「いま適用されている値」はウィンドウに 1 つしか無く、タブを切り替えても付いていかない——漏斗ボタンのドット（`filterActive`）が別のタブのフィルタを指したままになり、そこで Apply を押すと古い値が新しいタブへ入る。正しく直すには**いま適用されているフィルタを C++ から読めるようにする**必要があり、そこが`evolve/045` の範囲を出るので別項目にした |
| todo | タイトルバーとタスクバーにアプリアイコンを表示する | S | Phase 17a/17b の自前キャプション行へ。**アイコンの用意待ちで止めない**——とりあえず適当な丸い画像を暫定で入れておき、本物ができたら差し替える。タスクバー側も同時に（`QGuiApplication::setWindowIcon` で片付くなら 1 件、別物なら分ける） |
| todo | 書庫ファイルの中身一覧プレビュー（zip / 7z / rar） | L | 受信箱より。書庫ファイルを選ぶと、右のプレビューペインに中のエントリ構造（ツリー）が出る。**展開はしない**（中身の取り出しも、中のファイルのプレビューも対象外）。3 形式で難易度が大きく違い、zip だけは概念検証済み。着手前に S/M へ分割すること → `docs/investigations/STUDY_ARCHIVE_PREVIEW.md`（§0 が結論、§8 が PoC で分かったこと） |

---

## 優先度: 低

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| todo | 多言語対応（日本語） | L | 現在英語のみ。全 QML の文字列を `qsTr` に通す作業量が本体。着手前に S/M へ分割すること。**設定画面の「言語」セレクタもここに含める**——`evolve/041` の設定画面はテーマだけで出した（`.ts` も `QTranslator` も無い段階では選択肢が 1 つしか無いため）。置き場は `SettingsDialog.qml` の GridLayout に 1 行足すだけ |
| todo | アルバム | L | MEGA のアルバムの表示・作成・削除・出し入れ。**UI をどうするかが未決**なので、分割の前に仕様を決める必要がある。枠組みは `STUDY_SPECIAL_VIEWS_FRAMEWORK.md` |
| todo | リアルタイム反映 | L | 他デバイスの変更を SDK のプッシュ通知で開いている一覧へ。Phase 6 のフォルダオープン時バックグラウンド更新に足す形。**スコープ宣言（「同期クライアントではない」）との線引きを先に決める** |
| todo | アプリ外へのドラッグ&ドロップ | L | アプリ内のアイテムを Windows Explorer へドラッグしてダウンロード。Win32 の `IDataObject` 実装が要る |
| todo | Mica / Acrylic の背景を適用する | L | `FluentWinUI3` のパレットと合わせるのが手間で後回しにしている。見た目の作業なので**ループのスコープ外**（`docs/DESIGN_IMPROVEMENT.md` 側の話） |
| todo | 詳細検索 — スコープ切替（開いているフォルダ配下 / アカウント全体） | M | `MegaApi::search(MegaSearchFilter*, ...)` が再帰、`getChildren(filter, ...)` が非再帰。現状の検索がどちらを使っているか確認してから。**優先度「中」から降格**（Cloud Drive を開いて検索すれば足りるため） |
| todo | ラベル（色）の設定 | M | 受信箱の [編集] より（旧「ラベル（色）の設定と表示」を設定／表示に分割し、「優先度: 中」から降格）。コンテキストメニューに「ラベル」を追加し、ネストしたメニューへ 未設定 / 赤 / オレンジ / 黄色 / 緑 / 青 / 紫 / グレー を並べる。各項目の左に色付きの丸アイコン（Fluent Icons `ea3b` CircleFill）。MEGA のノードはラベルを持てるので、設定の口を **SDK 層**（`IMegaClient`）に足すところから |
| todo | ラベル（色）の表示 | M | 受信箱の [編集] より（同上の分割）。**リスト表示**: 行の一番左端（アイコンの左）に 3px の縦棒を色で描く（`\| [名前] [更新日時] [サイズ]` の先頭の縦棒を色付けする形）。ラベル無しなら塗らないので、その行だけ左マージンが 3px 増えて見えるが**それでよい**。今の 8px ほどの左マージンはそのまま。**グリッド表示**: ファイル名の左に色付きの丸アイコン（`ea3b` CircleFill）。アイコン用の場所を先に空けておく必要はなく、有無で名前の位置がずれてよい。`FileEntry` がラベルを運ぶ必要があるので「ラベル（色）の設定」と同じ土台に乗る |

---

## 見送り (blocked)

**ここはループが着手しない。提案もしない。**

| 状態 | 項目 | サイズ | メモ |
|---|---|---|---|
| blocked | 汎用の「もとに戻す」（undo） | L | **スコープ外。** `evolve/056` で入った**「移動・削除トーストの『もとに戻す』」はこれとは別物**で、履歴を持たずトーストが指す 1 操作だけを戻すもの。混同しないこと。 MEGA にネイティブな undo が無く、全操作に手書きの逆操作（rename↔rename / rubbish→戻す / copy→delete / create→delete）と、全変更経路への記録フック、履歴破棄のポリシーが要る。Phase 23（コピー／カット／貼り付け）と同じ要望から出たが、そこで落とした。→ `docs/PROGRESS.md` の「Phases 20a–23 — the detail pass」前文。`CLAUDE.md` のスコープ宣言もこれに従う |
| blocked | ローカルとの完全な双方向同期 | L | **スコープ外。** このアプリは同期クライアントではない（フォルダを開いたときの単発バックグラウンド更新だけ） |
| blocked | タブをウィンドウの外へ切り離す | L | **スコープ外。** → `docs/investigations/STUDY_TITLEBAR_TABS.md` |
| blocked | プラグイン対応 | L | **保留（スコープ外ではない）。** 現状未知の部分が多く大幅な機能改修が予想されるため、十分な仕様が固まるまで着手しない。中身: コンテキストメニュー項目の追加・独自プレビュー・設定画面の追加を外部から。トースト／ダイアログ／ファイル更新の API 公開も要る。動機は WD Tagger による自動タグ付けで、ファイラーのドメインから外れるためデフォルト同梱にしたくない、というもの |
| blocked | お気に入りでの並び替え | M | **保留（スコープ外ではない）。** 並び替え／フィルタを出すいい導線が思いつかず、適当に作ると邪魔になるので、UI が決まるまで止める。技術的な地雷は別にあって、`ORDER_FAV_ASC/DESC` はソートではなく**グループ化**なので、既存の「列 → ソートキー」の対応には乗らない |
